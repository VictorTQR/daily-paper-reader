---
title: "Combee: Scaling Prompt Learning for Self-Improving Language Model Agents"
title_zh: Combee：面向自我改进语言模型智能体的大规模并行提示学习
authors: "Hanchen Li, Runyuan He, Qizheng Zhang, Changxiu Ji, Qiuyang Mang, Xiaokun Chen, Lakshya A Agrawal, Wei-Liang Liao, Eric Yang, Alvin Cheung, James Zou, Kunle Olukotun, Ion Stoica, Joseph E. Gonzalez"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04247v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 为自我改进的大模型智能体扩展提示学习
tldr: 针对现有提示词学习方法在处理大规模智能体轨迹时效率低下且高并行下质量下降的问题，本文提出Combee框架。Combee通过并行扫描、增强洗牌机制和动态批大小控制器，实现了在大规模并行环境下的高效提示词学习。实验表明，Combee在保持或提升准确率的同时，比现有方法提速高达17倍，且成本相当，为自进化智能体的大规模扩展提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04247v1/fig-001.webp\", \"caption\": \"Figure 2: Context overload from naive scaling. As batch size increases, the aggregator LLM produces monotonically fewer and less useful context updates, directly degrading final accuracy across benchmarks.\", \"page\": 3, \"index\": 1, \"width\": 739, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2604.04247v1/fig-002.webp\", \"caption\": \"Figure 4: Combee achieves superior quality-delay trade off on GEPA for finance benchmarks.\", \"page\": 8, \"index\": 2, \"width\": 822, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.04247v1/fig-003.webp\", \"caption\": \"Figure 5: Combee achieves superior quality-delay trade off on ACE for finance benchmarks.\", \"page\": 8, \"index\": 3, \"width\": 797, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.04247v1/fig-004.webp\", \"caption\": \"Figure 3: Overall design of Combee (top) vs. naive scaling (bottom). Combee follows a Map-Shuffle-Reduce paradigm: the Map phase dispatches n parallel agents to execute queries and reflect; the Shuffle phase applies augmented shuffling; and the Reduce phase hierarchically combines reflections via parallel scan aggregation. In contrast, naive scaling feeds all reflections directly into a single prompt update, causing context overload.\", \"page\": 5, \"index\": 4, \"width\": 732, \"height\": 552}]"
motivation: 现有的提示词学习方法难以高效处理大规模并行智能体产生的海量轨迹，且在高并行设置下往往会出现性能退化。
method: 提出Combee框架，利用并行扫描、增强洗牌机制以及动态批大小控制器来平衡学习质量与延迟。
result: 在AppWorld和Terminal-Bench等多个基准测试中，Combee实现了最高17倍的加速，同时保持了与现有方法相当或更优的准确率。
conclusion: Combee成功解决了自改进智能体在提示词学习中的扩展性难题，实现了高效且高质量的大规模并行学习。
---

## 摘要
最近提示学习（prompt learning）的进展使得大语言模型智能体能够在不改变参数的情况下，从推理时上下文（inference-time context）中获取任务相关知识。例如，现有方法（如 ACE 或 GEPA）可以根据之前的智能体运行记录学习系统提示词，从而提高准确率。然而，这些方法主要关注单智能体或低并行度的设置，这从根本上限制了它们从大量收集的智能体轨迹（agentic traces）中高效学习的能力。为了适应从大量智能体轨迹或并行智能体执行中学习的趋势，并行运行提示学习将是高效且有益的。然而，由于缺乏原则性的扩展策略，现有方法在高并行度下会出现质量下降。为了同时提高提示学习的效率和质量，我们提出了 Combee，这是一个为自我改进智能体扩展并行提示学习的新型框架。Combee 加速了学习过程，并支持在并行运行多个智能体的同时，从它们的聚合轨迹中学习且不损失质量。为实现这一目标，Combee 利用了并行扫描（parallel scans）并采用了增强的洗牌机制（augmented shuffle mechanism）；此外，Combee 还引入了动态批大小控制器（dynamic batch size controller）以平衡质量和延迟。在 AppWorld、Terminal-Bench、Formula 和 FiNER 上的评估表明，Combee 在保持相当或更高准确率以及同等成本的前提下，比之前的方法实现了高达 17 倍的加速。

## Abstract
Recent advances in prompt learning allow large language model agents to acquire task-relevant knowledge from inference-time context without parameter changes. For example, existing methods (like ACE or GEPA) can learn system prompts to improve accuracy based on previous agent runs. However, these methods primarily focus on single-agent or low-parallelism settings. This fundamentally limits their ability to efficiently learn from a large set of collected agentic traces. It would be efficient and beneficial to run prompt learning in parallel to accommodate the growing trend of learning from many agentic traces or parallel agent executions. Yet without a principled strategy for scaling, current methods suffer from quality degradation with high parallelism. To improve both the efficiency and quality of prompt learning, we propose Combee, a novel framework to scale parallel prompt learning for self-improving agents. Combee speeds up learning and enables running many agents in parallel while learning from their aggregate traces without quality degradation. To achieve this, Combee leverages parallel scans and employs an augmented shuffle mechanism; Combee also introduces a dynamic batch size controller to balance quality and delay. Evaluations on AppWorld, Terminal-Bench, Formula, and FiNER demonstrate that Combee achieves up to 17x speedup over previous methods with comparable or better accuracy and equivalent cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Combee: Scaling Prompt Learning for Self-Improving Language Model Agents》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文关注的是**大语言模型（LLM）智能体的自我改进能力**。目前，如 ACE 或 GEPA 等方法通过“生成-反思-更新”循环，从推理时的交互轨迹中提取知识并更新系统提示词（Prompt Learning），从而提升性能。
*   **核心痛点：** 现有的提示学习方法主要针对单智能体或低并行场景。当尝试通过增加批处理大小（Batch Size）来并行处理大量轨迹时，会出现**“上下文过载”（Context Overload）**现象。
*   **现象描述：** 即使所有反思（Reflections）都在模型的上下文窗口内，聚合器 LLM 也会因为信息密度过大而进行“有损压缩”，丢弃具体的、高价值的洞察，只保留泛泛而谈的模式，导致智能体性能大幅下降。
*   **研究目标：** 开发一种能够在大规模并行环境下，既能保持学习质量又能显著提升训练效率的提示学习框架。

### 2. 论文提出的方法论
Combee 采用了 **Map-Shuffle-Reduce** 范式，核心包含三个关键组件：
*   **并行扫描聚合（Parallel Scan Aggregation）：** 借鉴并行计算中的前缀和思想，Combee 不再一次性将所有反思喂给 LLM，而是构建一个多层聚合树。它将 $n$ 个轨迹分成 $\sqrt{n}$ 个小组，先在小组内聚合生成局部更新，再将这些局部更新分层合并。这种分层处理有效避免了单次处理信息量过大的问题。
*   **增强洗牌机制（Augmented Shuffling）：** 为了防止关键信息在聚合过程中丢失，Combee 将每个生成的反思进行多次复制（默认 $p=2$）并随机洗牌。这增加了每个洞察被聚合器捕捉到的机会，类似于集成学习中的自洽性原则。
*   **动态批大小控制器（Dynamic Batch Size Controller）：** 自动平衡训练延迟与质量。系统通过预先测试不同批大小的延迟，拟合幂律延迟曲线，找到边际收益递减的临界点，从而在保证质量的前提下选择最优的并行度。

### 3. 实验设计
*   **数据集与场景：**
    *   **智能体基准：** AppWorld（多步 API 任务）、Terminal-Bench 2.0（命令行软件工程任务）。
    *   **领域特定基准：** FiNER（金融实体识别）、Formula（金融数值推理）。
*   **对比方法：**
    *   **基础模型：** 无提示学习的 Base LLM。
    *   **基准框架：** 原始的 ACE 和 GEPA（在不同固定批大小下）。
    *   **启发式策略：** Top-K 检索（基于嵌入聚类筛选反思）、摘要法（先总结再聚合）。
*   **评估指标：** 任务成功率（TGC/SGC）、准确率（Accuracy）、训练时间、训练成本。

### 4. 资源与算力
*   **模型使用：** 主要实验基于 **DeepSeek-V3.1**（通过 Together AI 接口调用），部分实验使用了 **GPT-OSS 120B** 以验证跨模型鲁棒性。
*   **算力细节：** 论文未详细列出具体的物理 GPU 数量（因为主要使用 API 服务），但记录了详细的**训练时长**。例如，在 AppWorld 上，Combee 将训练时间从 86 分钟缩短至 7 分钟；在 Terminal-Bench 上实现了 17 倍的加速。
*   **成本：** 实验证明 Combee 在大幅提速的同时，其 API 调用成本与原始顺序学习方法基本持平。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 个不同性质的基准测试上进行了实验，涵盖了从复杂的 API 调用到精细的数值推理。
*   **消融实验：** 专门针对“增强洗牌”和“动态批大小控制器”进行了消融分析，验证了各组件的必要性。
*   **充分性评价：** 实验设计较为充分。作者不仅对比了最终准确率，还通过**定性分析**（展示 Playbook 条目的变化）和**定量分析**（统计有效条目的命中次数）深入揭示了“上下文过载”的本质。跨模型的验证（DeepSeek 和 GPT-OSS）也增强了结论的普遍性。

### 6. 主要结论与发现
*   **打破效率-质量权衡：** Combee 证明了在大规模并行（如 Batch Size 40+）下，通过合理的结构化聚合，可以获得与顺序学习（Batch Size 1）相当甚至更好的准确率。
*   **显著提速：** 在多个任务中实现了 12x 到 17x 的端到端训练加速。
*   **上下文过载是真实存在的：** 实验证实，简单的增加批大小会导致 LLM 丢失 90% 以上的高价值知识条目，而 Combee 的分层聚合能保留这些关键信息。

### 7. 优点（亮点）
*   **框架无关性：** Combee 可以无缝集成到现有的“生成-反思-更新”类框架（如 ACE, GEPA, DSPy 等）中。
*   **系统性解决扩展性：** 首次系统性地识别并解决了提示学习中的并行扩展瓶颈，而非简单的 Prompt Engineering。
*   **动态自适应：** 引入动态批大小控制，减少了人工调参的负担。

### 8. 不足与局限
*   **同步限制：** 目前的设计假设每一轮迭代内是同步并行执行的，未来若能支持异步更新（类似异步 SGD），效率可能进一步提升。
*   **模型依赖：** 虽然验证了两个模型，但对于更小规模或上下文处理能力较弱的模型，其分层聚合的参数（如小组大小）可能需要更精细的调整。
*   **任务类型：** 实验主要集中在逻辑、编程和金融领域，对于创意写作或纯感性交互类任务的适用性尚未验证。

（完）
