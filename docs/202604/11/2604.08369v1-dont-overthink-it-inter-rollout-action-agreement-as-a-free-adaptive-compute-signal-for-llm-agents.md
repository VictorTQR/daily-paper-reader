---
title: "Don't Overthink It: Inter-Rollout Action Agreement as a Free Adaptive-Compute Signal for LLM Agents"
title_zh: 别想太多：轨迹间动作一致性作为大语言模型智能体的免费自适应计算信号
authors: Khushal Sethi
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08369v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: LLM智能体的自适应计算
tldr: 针对LLM智能体在推理时计算资源分配不均的问题，本文提出TrACE。这是一种无需训练的自适应计算控制器，通过衡量多次采样动作的一致性来动态分配计算量：在一致性高的简单步骤快速决策，在一致性低的复杂步骤增加采样。实验表明，TrACE在保持与固定预算自一致性方法相当的准确率时，显著减少了LLM调用次数，为多步决策任务提供了高效的推理方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08369v1/fig-001.webp\", \"caption\": \"Figure 1: Compute–accuracy frontier. Each point is one (condition, benchmark) pair. TrACE-4 and TrACE-8 (red, pink) match the accuracy of SC-4 and SC-8 (blue) at substantially fewer LLM calls, Pareto-dominating the self-consistency baseline in both settings. Error bars on MiniHouse are 95% bootstrap CIs; GSM8K bars are Wald CIs from binomial data (no per-task data available for bootstrap, see §6).\", \"page\": 4, \"index\": 1, \"width\": 747, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.08369v1/fig-002.webp\", \"caption\": \"Figure 2: Left: Agreement is a reliable success signal. Right: TrACE call counts are task-adaptive.\", \"page\": 7, \"index\": 2, \"width\": 832, \"height\": 376}, {\"url\": \"assets/figures/arxiv/2604.08369v1/fig-003.webp\", \"caption\": \"Figure 3: τhigh ablation (TrACE-4, kmax = 4). Three thresholds are evaluated: 0.50 (commit at bare majority), 0.75 (default), and 1.00 (require unanimity, degenerates to SC-4). The dotted line shows SC-4 accuracy and call count for reference. TrACE with τ = 0.75 achieves matched accuracy at substantially fewer calls than either extreme. τ = 0.50 underspends and slightly loses accuracy; τ = 1.00 recovers SC-4 accuracy and cost.\", \"page\": 8, \"index\": 3, \"width\": 812, \"height\": 260}]"
motivation: 现有的推理计算缩放方法对所有决策步骤统一分配预算，忽略了任务难度的差异，导致计算资源浪费。
method: 提出TrACE控制器，通过测量候选动作的一致性来判断决策难度，从而动态决定是否需要增加采样次数。
result: "在GSM8K和MiniHouse基准测试中，TrACE在达到与固定采样方法相同精度的前提下，减少了33%至65%的LLM调用量。"
conclusion: 动作一致性是衡量步骤难度的可靠信号，无需额外训练或验证器即可实现高效的自适应推理计算分配。
---

## 摘要
推理时计算缩放已成为提高大语言模型（LLM）智能体可靠性的强大技术，但现有方法通常统一分配计算资源：无论难度如何，每个决策步骤都获得相同的预算。我们引入了 TrACE（通过一致性实现的轨迹自适应计算），这是一种无需训练的控制器，通过测量轨迹间动作一致性，在智能体的时间步之间自适应地分配 LLM 调用次数。在每一步中，TrACE 采样一小组候选下一步动作，并测量模型选择同一动作的一致性程度。高一致性预示着决策简单，控制器会立即提交动作；低一致性则预示着不确定性，控制器会继续采样额外的轨迹，直到达到可配置的上限，然后提交多数票选出的动作。该方法不需要学习组件、外部验证器或人类标签。我们在涵盖单步推理（GSM8K，n=50）和多步家庭导航（MiniHouse，n=30）的两个基准测试上，使用在 CPU 上运行的 Qwen 2.5 3B Instruct 模型，将 TrACE 与贪婪解码和固定预算自一致性（SC-4, SC-8）进行了对比评估。结果显示，TrACE-4 在 GSM8K 和 MiniHouse 上分别减少了 33% 和 39% 的 LLM 调用，且准确率与 SC-4 持平；TrACE-8 在 GSM8K 和 MiniHouse 上分别减少了 55% 和 65% 的调用，且准确率与 SC-8 持平。我们进一步证明，轨迹间一致性是步骤级成功的可靠信号，验证了核心假设：模型自身的输出一致性编码了可以在无需训练的情况下利用的难度信息。TrACE 是首个在多步顺序决策任务上进行评估的、针对 LLM 智能体的无需训练、逐时间步自适应计算控制器。

## Abstract
Inference-time compute scaling has emerged as a powerful technique for improving the reliability of large language model (LLM) agents, but existing methods apply compute uniformly: every decision step receives the same budget regardless of its difficulty. We introduce TrACE (Trajectorical Adaptive Compute via agrEement), a training-free controller that allocates LLM calls adaptively across agent timesteps by measuring inter-rollout action agreement. At each step, TrACE samples a small set of candidate next actions and measures how consistently the model commits to the same action. High agreement signals an easy decision; the controller commits immediately. Low agreement signals uncertainty; the controller samples additional rollouts up to a configurable cap before committing to the plurality action. No learned components, no external verifier, and no human labels are required. We evaluate TrACE against greedy decoding and fixed-budget self-consistency (SC-4, SC-8) on two benchmarks spanning single-step reasoning (GSM8K, n=50) and multi-step household navigation (MiniHouse, n=30), using a Qwen 2.5 3B Instruct model running on CPU. TrACE-4 matches SC-4 accuracy while using 33% fewer LLM calls on GSM8K and 39% fewer on MiniHouse. TrACE-8 matches SC-8 accuracy with 55% fewer calls on GSM8K and 65% fewer on MiniHouse. We further show that inter-rollout agreement is a reliable signal of step-level success, validating the core hypothesis that the model's own output consistency encodes difficulty information that can be exploited without training. TrACE is the first training-free, per-timestep adaptive-compute controller for LLM agents to be evaluated on multi-step sequential decision tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Don't Overthink It: Inter-Rollout Action Agreement as a Free Adaptive-Compute Signal for LLM Agents》的结构化深入分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）智能体在执行多步任务时，每个决策步骤的难度差异巨大。然而，现有的推理时计算缩放方法（如 Self-Consistency）通常对所有步骤分配统一的计算预算（固定的采样次数），导致“简单步骤浪费资源，复杂步骤资源不足”。
*   **核心问题**：如何在无需额外训练、无需外部验证器的情况下，让 LLM 智能体能够根据当前步骤的难度，自适应地分配推理计算资源？

### 2. 方法论：TrACE (Trajectorical Adaptive Compute via agrEement)
*   **核心思想**：利用模型自身输出的“动作一致性”（Agreement）作为难度的代理信号。如果模型在多次随机采样中高度趋同于同一个动作，说明该决策简单；反之则说明决策困难，需要更多采样来通过“多数投票”纠偏。
*   **算法流程**：
    1.  **初始采样**：在每个时间步 $t$，先以温度 $\tau > 0$ 独立采样 $k_{init}$（默认 2）个候选动作。
    2.  **计算一致性**：计算这组动作中出现频率最高的动作（众数）所占的比例 $\alpha_t$。
    3.  **自适应扩展**：
        *   若 $\alpha_t \ge \tau_{high}$（预设阈值，如 0.75），则认为达成共识，立即提交该动作并进入下一步。
        *   若 $\alpha_t < \tau_{high}$，则继续逐个采样新的动作，直到一致性达到阈值或采样总数达到上限 $k_{max}$（如 4 或 8）。
    4.  **规范化处理**：使用动作规范化器（Canonicaliser）处理字符串（去空格、转小写、去标点），确保一致性计算不受表面格式影响。

### 3. 实验设计
*   **数据集/场景**：
    *   **GSM8K**：小学数学应用题，用于测试单步推理中的一致性信号。
    *   **MiniHouse**：作者自建的轻量级文本环境，模拟家庭导航和物体操作（如“去厨房拿苹果放进冰箱”），涉及多步顺序决策。
*   **Benchmark 与对比方法**：
    *   **Greedy**：贪婪解码（k=1）。
    *   **Self-Consistency (SC-4, SC-8)**：固定采样 4 次或 8 次的多数投票法。
    *   **TrACE-4, TrACE-8**：本文提出的自适应方法，上限分别为 4 和 8。

### 4. 资源与算力
*   **模型**：Qwen 2.5 3B Instruct，使用 Q4_K_M 量化（GGUF 格式）。
*   **硬件**：Apple M 系列 CPU（32GB RAM），使用 8 个推理线程。
*   **特点**：实验强调“CPU 友好型”和“低算力可复现性”，未动用大规模 GPU 集群，证明了该方法在端侧或轻量级设备上的实用性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   GSM8K 抽取了 50 个测试样本。
    *   MiniHouse 包含 30 个任务，每个任务运行 3 个随机种子（共 90 次实验）。
*   **消融实验**：针对关键超参数——一致性阈值 $\tau_{high}$ 进行了消融研究（对比了 0.50, 0.75, 1.00 三种设置）。
*   **充分性评价**：实验覆盖了单步与多步任务，并提供了 95% 置信区间。虽然样本绝对数量（n=30/50）不算巨大，但对于验证“推理效率提升”这一核心主张而言是充分且客观的。

### 6. 主要结论与发现
*   **帕累托改进**：TrACE 在保持与固定预算 SC 方法相同准确率的前提下，显著减少了 LLM 调用次数。
    *   在 GSM8K 上，TrACE-8 比 SC-8 减少了 **55%** 的调用。
    *   在 MiniHouse 上，TrACE-8 比 SC-8 减少了 **65%** 的调用。
*   **信号可靠性**：实验证实，动作一致性 $\alpha_t$ 与步骤的最终成功率呈正相关，证明了其作为难度指示器的有效性。
*   **时间节省**：在 CPU 环境下，TrACE-8 的实际墙钟时间比 SC-8 缩短了约 65%。

### 7. 优点
*   **无需训练（Training-free）**：不需要昂贵的奖励模型（PRM/ORM）或微调，直接应用于任何开源模型。
*   **即插即用**：作为推理层控制器，不改变模型权重，易于集成到现有智能体框架中。
*   **效率极高**：通过“早停”机制，在简单任务上极大地节省了计算开销。
*   **开源贡献**：提供了 MiniHouse 这一无需复杂依赖（如 C++ 编译）的轻量级智能体评测环境。

### 8. 不足与局限
*   **模型规模局限**：实验仅限于 3B 参数模型，尚未在大规模模型（如 70B+）上验证一致性信号的校准效果。
*   **任务复杂度**：MiniHouse 任务相对简单，且模型在多步规划上存在性能瓶颈（准确率在 0.37 左右触顶），未能展示该方法在极高难度任务下的表现。
*   **开放式生成挑战**：该方法依赖于动作空间的离散化或规范化，对于长文本生成或代码编写等动作空间近乎无限的任务，计算“一致性”会变得更加困难。
*   **阈值敏感性**：虽然 0.75 是一个较好的默认值，但最佳阈值可能随模型和任务的变化而波动。

（完）
