---
title: Residuals-based Offline Reinforcement Learning
title_zh: 基于残差的离线强化学习
authors: "Qing Zhu, Xian Yu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01378v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 离线强化学习贝尔曼算子的基础理论
tldr: 针对离线强化学习中数据覆盖受限和分布偏移的挑战，本文提出了一种基于残差的离线强化学习框架。该框架通过定义一个结合经验残差的贝尔曼最优算子，将转移动力学学习中的估计误差显式引入策略优化。研究证明了该算子的收缩性与渐近最优性，并开发了相应的离线DQN算法，在随机CartPole环境中验证了其有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01378v1/fig-001.webp\", \"caption\": \"Fig. 1: Flowchart of the residuals-based offline RL.\", \"page\": 3, \"index\": 1, \"width\": 512, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.01378v1/fig-002.webp\", \"caption\": \"Fig. 2: Comparison of different sample sizes\", \"page\": 7, \"index\": 2, \"width\": 429, \"height\": 577}, {\"url\": \"assets/figures/arxiv/2604.01378v1/fig-003.webp\", \"caption\": \"Fig. 3: Comparison of Models\", \"page\": 8, \"index\": 3, \"width\": 383, \"height\": 516}]"
motivation: 旨在解决离线强化学习中对数据覆盖要求过高以及模型学习中分布偏移导致的性能下降问题。
method: 定义了一种基于残差的贝尔曼最优算子，利用经验残差来量化并补偿转移概率估计中的误差。
result: 理论证明了该算子的收缩映射特性及有限样本保证，并在随机CartPole实验中展示了算法的优越性。
conclusion: 该框架为在不完全数据覆盖下实现稳健的离线策略优化提供了一种具有理论保障的新方法。
---

## 摘要
离线强化学习（RL）因其能够从先前收集的数据中学习策略而无需与真实环境交互而受到越来越多的关注，这在具有高风险的应用中尤为重要。尽管已有大量工作开发了离线强化学习算法，但这些方法通常依赖于对数据覆盖范围的限制性假设，并受到分布偏移的影响。在本文中，我们针对一般状态和动作空间提出了一种基于残差的离线强化学习框架。具体而言，我们定义了一个基于残差的贝尔曼最优算子，该算子通过利用经验残差，将学习转移动态过程中的估计误差显式地纳入策略优化中。我们证明了该贝尔曼算子是一个收缩映射，并确定了其不动点在何种条件下具有渐近最优性并具备有限样本保证。我们进一步开发了一种基于残差的离线深度Q网络（DQN）算法。通过使用随机倒立摆（CartPole）环境，我们证明了基于残差的离线DQN算法的有效性。

## Abstract
Offline reinforcement learning (RL) has received increasing attention for learning policies from previously collected data without interaction with the real environment, which is particularly important in high-stakes applications. While a growing body of work has developed offline RL algorithms, these methods often rely on restrictive assumptions about data coverage and suffer from distribution shift. In this paper, we propose a residuals-based offline RL framework for general state and action spaces. Specifically, we define a residuals-based Bellman optimality operator that explicitly incorporates estimation error in learning transition dynamics into policy optimization by leveraging empirical residuals. We show that this Bellman operator is a contraction mapping and identify conditions under which its fixed point is asymptotically optimal and possesses finite-sample guarantees. We further develop a residuals-based offline deep Q-learning (DQN) algorithm. Using a stochastic CartPole environment, we demonstrate the effectiveness of our residuals-based offline DQN algorithm.

---

## 论文详细总结（自动生成）

这是一份关于论文《Residuals-based Offline Reinforcement Learning》（基于残差的离线强化学习）的结构化总结：

### 1. 论文的核心问题与整体含义
离线强化学习（Offline RL）旨在从静态数据集中学习策略，而无需与环境交互。然而，现有方法面临三大核心挑战：
*   **数据覆盖限制**：通常假设数据集需覆盖所有状态-动作空间，这在连续空间中难以实现。
*   **反事实查询困难**：难以预测未执行动作的后果。
*   **分布偏移**：训练策略与行为策略不一致导致价值评估不可靠。

**研究动机**：借鉴上下文随机优化中的“经验残差样本平均近似（ER-SAA）”思想，通过显式建模转移动力学的估计误差（残差），来增强离线策略的稳健性和泛化能力。

### 2. 论文提出的方法论
该论文提出了一种**基于残差的离线强化学习框架**，核心步骤如下：
*   **回归建模**：利用监督学习从离线数据集 $D_N$ 中拟合转移函数 $\hat{f}_N(s, a)$，预测下一状态。
*   **残差提取**：计算观测到的真实状态与预测状态之间的经验残差：$\hat{\epsilon}^{(i)}_N = s'^{(i)} - \hat{f}_N(s^{(i)}, a^{(i)})$。
*   **残差贝尔曼算子**：定义了一个新的算子 $\hat{T}_N$，在计算未来价值期望时，通过在点预测值上叠加随机抽样的经验残差来模拟环境的随机性：
    $$\hat{s}' = \Pi_S(\hat{f}_N(s, a) + \hat{\epsilon}_t)$$
*   **理论保证**：论文证明了该算子是**收缩映射**，确保了不动点的唯一性；并证明了其在满足一致性假设下具有**渐近最优性**和**有限样本误差界限**。
*   **算法实现**：开发了**基于残差的离线DQN算法**，将上述逻辑集成到深度Q网络训练中。

### 3. 实验设计
*   **实验场景**：随机 CartPole（倒立摆）环境。为了模拟真实世界的随机性，在车杆位置观测中加入了高斯噪声 $\mathcal{N}(0, 0.25)$。
*   **Benchmark（基准）**：对比了“无残差模型”（即仅使用回归模型 $\hat{f}_N$ 进行点预测，不考虑误差分布）。
*   **对比维度**：
    1.  **样本量敏感度**：测试了离线轨迹数量 $n \in \{500, 600, \dots, 1000\}$ 对性能的影响。
    2.  **模型有效性**：对比有无残差补偿在训练集和测试集上的表现。

### 4. 资源与算力
*   **网络结构**：转移模型使用单隐藏层（64神经元）神经网络；Q网络使用双隐藏层（每层64神经元）神经网络。
*   **超参数**：Adam优化器，学习率 $10^{-4}$，折扣因子 $\gamma=0.99$，$\epsilon$-greedy 衰减策略。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。鉴于 CartPole 任务的规模，预计在普通消费级 GPU 或 CPU 上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：针对不同样本量进行了多组对比实验，每组实验均进行了 **5 次独立运行**并取平均值。
*   **充分性评价**：
    *   **理论验证充分**：实验结果验证了样本量增加能提升模型质量，且残差项对处理随机动力学至关重要。
    *   **环境单一**：实验仅限于 CartPole 这一基础环境，缺乏在更复杂的离线 RL 基准集（如 D4RL）上的验证，因此其在复杂高维任务中的泛化能力尚待观察。

### 6. 论文的主要结论与发现
*   **残差的重要性**：单纯依靠回归模型（无残差）虽然在训练时表现尚可，但在真实随机环境中表现极差。引入经验残差能显著提升策略在真实环境中的测试奖励。
*   **数据规模效应**：随着离线数据量的增加，转移模型的预测精度和残差分布的代表性随之提升，最终策略性能也稳步提高。
*   **理论与实践一致**：实验结果支持了论文关于残差贝尔曼算子收缩性和一致性的理论推导。

### 7. 优点
*   **理论严谨**：为离线 RL 提供了一套完整的基于残差的数学框架，并给出了收缩映射和收敛性的严格证明。
*   **无需全覆盖假设**：通过残差抽样自动生成“未见状态”，缓解了离线 RL 对数据集覆盖范围的苛刻要求。
*   **即插即用**：该框架可以较容易地扩展到其他基于值的 RL 算法（如 Rainbow 或 SAC）中。

### 8. 不足与局限
*   **实验覆盖面窄**：仅在简单的随机 CartPole 环境中测试，未在 MuJoCo 或其他具有挑战性的离线 RL 任务中进行对比。
*   **残差独立性假设**：理论分析中部分假设（如残差与状态的独立性或特定分布性质）在极度非线性的复杂系统中可能难以完全满足。
*   **计算开销**：在训练过程中需要维护残差集并进行投影操作，对于超大规模数据集，抽样和存储残差可能会带来额外的计算负担。

（完）
