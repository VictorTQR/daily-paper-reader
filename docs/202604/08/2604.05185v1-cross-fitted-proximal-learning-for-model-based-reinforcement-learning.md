---
title: Cross-fitted Proximal Learning for Model-Based Reinforcement Learning
title_zh: 基于模型的强化学习中的交叉拟合近端学习
authors: "Nishanth Venkatesh, Andreas A. Malikopoulos"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05185v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 含有混杂因素的POMDP中的基于模型的强化学习
tldr: 本研究针对存在隐性混杂因素的离线模型强化学习（POMDPs）中模型估计偏差问题，提出了一种交叉拟合的近端学习方法。该方法通过估计满足条件矩限制（CMR）的奖励-发射和观测-转移桥函数，并结合K折交叉拟合技术，提高了数据利用效率。研究还推导了该估计器的理论误差界限，并将其分解为干扰项估计误差和经验平均误差，为复杂环境下的策略评估提供了更稳健的统计工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05185v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of observation-transition recovery\", \"page\": 6, \"index\": 1, \"width\": 512, \"height\": 325}]"
motivation: 在存在隐性混杂因素的离线部分可观测马尔可夫决策过程中，直接从观测数据学习模型会导致偏差，需要更有效的桥函数估计方法。
method: 提出一种K折交叉拟合的二阶段桥函数估计器，将桥函数学习建模为包含干扰项（如条件均值嵌入和条件密度）的条件矩限制问题。
result: 建立了交叉拟合估计器的Oracle比较界限，并成功将误差分解为干扰项估计引起的阶段I误差和经验平均引起的阶段II误差。
conclusion: 该方法在保持桥函数识别策略的同时，显著提升了数据效率和统计估计的稳健性，为混杂环境下的模型强化学习提供了理论支撑。
---

## 摘要
基于模型的强化学习在序列决策中具有吸引力，因为它显式地估计奖励和状态转移模型，并随后通过模拟展开支持规划。然而，在存在隐性混杂因素的离线设置中，直接从观测数据学习的模型可能会产生偏差。这一挑战在部分可观测系统中尤为突出，其中潜在因素可能共同影响动作、奖励和未来的观测。最近的研究表明，在此类受混杂影响的部分可观测马尔可夫决策过程（POMDPs）中，策略评估可以简化为估计满足条件矩限制（CMRs）的奖励-发射和观测-转移桥函数。在本文中，我们研究了这些桥函数的统计估计。我们将桥函数学习建模为一个 CMR 问题，其冗余对象由条件均值嵌入和条件密度给出。随后，我们为现有的两阶段桥估计器开发了一种 K 折交叉拟合扩展。所提出的程序保留了原始的基于桥函数的识别策略，同时比单次样本分割更有效地利用了可用数据。我们还推导了交叉拟合估计器的 oracle-comparator 界，并将产生的误差分解为由冗余估计引起的阶段 I 项和由经验平均引起的阶段 II 项。

## Abstract
Model-based reinforcement learning is attractive for sequential decision-making because it explicitly estimates reward and transition models and then supports planning through simulated rollouts. In offline settings with hidden confounding, however, models learned directly from observational data may be biased. This challenge is especially pronounced in partially observable systems, where latent factors may jointly affect actions, rewards, and future observations. Recent work has shown that policy evaluation in such confounded partially observable Markov decision processes (POMDPs) can be reduced to estimating reward-emission and observation-transition bridge functions satisfying conditional moment restrictions (CMRs). In this paper, we study the statistical estimation of these bridge functions. We formulate bridge learning as a CMR problem with nuisance objects given by a conditional mean embedding and a conditional density. We then develop a $K$-fold cross-fitted extension of the existing two-stage bridge estimator. The proposed procedure preserves the original bridge-based identification strategy while using the available data more efficiently than a single sample split. We also derive an oracle-comparator bound for the cross-fitted estimator and decompose the resulting error into a Stage I term induced by nuisance estimation and a Stage II term induced by empirical averaging.

---

## 论文详细总结（自动生成）

以下是对论文《Cross-fitted Proximal Learning for Model-Based Reinforcement Learning》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：在离线强化学习（Offline RL）中，当系统处于**部分可观测（POMDP）**且存在**隐性混杂因素（Hidden Confounding）**时，直接从观测数据中学习到的奖励和转移模型会产生严重偏差。
*   **研究动机**：隐性因素可能同时影响动作选择和系统演化，导致观测数据中存在虚假相关性。现有的“桥函数”（Bridge Function）方法虽然能通过条件矩限制（CMR）识别系统机制，但在有限样本下，传统的两阶段估计器存在数据利用率低、对样本分割敏感等统计效率问题。

### 2. 方法论：交叉拟合近端学习
*   **核心思想**：将桥函数学习重新表述为一个包含**干扰项（Nuisance Objects）**的 CMR 问题，并引入 **K 折交叉拟合（K-fold Cross-fitting）** 技术来提高估计的稳健性和数据效率。
*   **关键技术细节**：
    *   **CMR 表述**：将奖励-发射桥和观测-转移桥的估计统一为寻找满足 $E[b(W, z) | C] = p(z | C)$ 的函数 $b$。
    *   **干扰项定义**：识别出两个关键干扰项——条件均值嵌入（CME，用于表示条件期望算子）和条件密度（用于表示目标分布）。
    *   **两阶段流程**：
        1.  **阶段 I（干扰项估计）**：在辅助样本（$K-1$ 折数据）上，利用正则化算子回归估计 CME，利用条件似然学习估计条件密度。
        2.  **阶段 II（桥函数估计）**：在留出的第 $K$ 折数据上，利用阶段 I 的估计值作为插件（Plug-in），构建经验风险函数并求解桥函数。
    *   **交叉拟合聚合**：对所有 $K$ 个折叠重复上述过程，并对结果进行平均，从而确保每个样本既用于干扰项估计也用于目标函数评估。
*   **理论贡献**：推导了 **Oracle-comparator 界限**，将总误差分解为由干扰项估计引起的“阶段 I 误差”和由经验过程引起的“阶段 II 误差”。

### 3. 实验设计
*   **实验场景**：一个 2 步时界的非线性高斯 POMDP 模拟环境。
*   **关键特征**：包含一个隐藏的二进制上下文因子，该因子同时影响行为策略（动作）和观测状态的演化，从而制造混杂偏差。
*   **Benchmark（对比基准）**：单次样本分割（One-shot 50-50 sample-splitting）基准方法。
*   **评估指标**：观测-转移恢复的均方误差（MSE），用于衡量模型对去混杂后系统动力学的还原能力。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件资源（如 GPU 型号、数量）或具体的训练时长。由于实验基于低维数值模拟，预计对算力的需求较低，普通的 CPU 或单块 GPU 即可完成。

### 5. 实验数量与充分性
*   **实验规模**：主要进行了一组随样本量 $N$（从 120 到 1920 不等）变化的对比实验。
*   **充分性评价**：
    *   **客观性**：实验展示了 MSE 随样本量增加而下降的趋势，符合统计学预期。
    *   **局限性**：实验仅限于合成的数值模拟环境，缺乏在复杂高维环境（如 Atari 或 MuJoCo）或真实世界数据集上的验证。
    *   **消融研究**：缺乏针对 $K$ 折叠数量、不同核函数选择或不同干扰项学习器的消融实验。

### 6. 主要结论与发现
*   **数据效率提升**：交叉拟合估计器在所有测试样本量下均显著优于单次分割基准。例如，在 $N=1920$ 时，MSE 从 0.0771 降至 0.0248。
*   **稳健性**：交叉拟合减少了估计器对特定样本划分的依赖，使结果更加稳定。
*   **理论验证**：实验结果支持了论文提出的误差分解理论，证明了改进第一阶段干扰项估计对降低整体误差的重要性。

### 7. 优点
*   **统计严谨性**：将双重机器学习（Double Machine Learning）的思想引入桥函数估计，提供了坚实的理论支撑。
*   **数据利用率**：通过交叉拟合解决了离线 RL 中数据稀缺的问题，避免了单次分割造成的数据浪费。
*   **通用性**：提出的框架可以灵活组合不同的核方法或深度学习模型来处理干扰项。

### 8. 不足与局限
*   **正交化缺失**：作者承认尚未推导出 **Neyman-正交得分（Neyman-orthogonal score）**，这意味着该方法目前对第一阶段估计误差的敏感度仍有优化空间。
*   **实验维度较低**：仅在简单的低维 POMDP 上进行了验证，尚未证明其在大规模深度强化学习任务中的可扩展性。
*   **超参数敏感性**：两阶段估计涉及多个正则化参数和核参数，论文未详细讨论这些参数的自动选择或敏感性。

（完）
