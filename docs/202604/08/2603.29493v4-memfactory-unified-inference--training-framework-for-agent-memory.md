---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v4"
tags: ["query:rl-agt"]
score: 10.0
evidence: 利用强化学习优化记忆增强型大模型的记忆操作
tldr: "MemFactory是首个针对记忆增强型智能体设计的统一、模块化训练与推理框架。针对现有记忆管理研究碎片化且缺乏通用基础设施的问题，该框架借鉴LLaMA-Factory的理念，将记忆生命周期抽象为可插拔组件，并集成GRPO算法以优化记忆策略。它支持多种前沿范式，显著降低了开发门槛，并在实验中实现了最高14.8%的性能提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v4/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v4/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强型大模型研究缺乏统一的基础设施，导致记忆操作的集成、训练和评估过程高度碎片化且难以复用。
method: 提出MemFactory框架，采用模块化“乐高式”架构抽象记忆生命周期，并原生集成GRPO算法通过环境奖励优化记忆管理策略。
result: "在MemAgent架构上的实验表明，MemFactory相比基础模型平均性能有所提升，最高相对增益达到14.8%。"
conclusion: MemFactory为记忆驱动的AI智能体提供了标准化、可扩展且易用的基础设施，有效降低了研究门槛并推动了该领域的创新。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各评估集上，MemFactory 相比相应的基座模型平均提升了性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：虽然大语言模型（LLM）在对话中表现出色，但要进化为真正的 AI 智能体，必须具备长期记忆管理能力。
*   **现有痛点**：
    *   传统的静态 RAG 或启发式规则难以处理冗余、信息冲突及长上下文限制。
    *   利用强化学习（RL）优化记忆操作（提取、更新、检索）虽具前景，但现有研究（如 Memory-R1, MemAgent）实现高度碎片化，缺乏统一的基础设施。
*   **核心目标**：开发一个类似 LLaMA-Factory 的统一框架，简化记忆增强型智能体的构建、训练与评估流程。

### 2. 方法论：核心思想与技术细节
MemFactory 提出了一个高度模块化的四层架构：
*   **Module Layer（模块层）**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：从原始上下文中解析并提取事实。
    *   **Updater**：执行 ADD、DEL、UPDATE、NONE 等操作维护记忆库。
    *   **Retriever**：负责语义检索及基于推理模型的重排序（Rerank）。
    *   **Agent Module**：支持端到端的递归记忆状态处理。
*   **Agent Layer（智能体层）**：采用“乐高式”组装模式，研究人员可通过组合不同模块构建自定义记忆策略。
*   **Environment Layer（环境层）**：负责数据加载与奖励管理。支持 `MemoryBankEnv`（长期记忆）和 `LongcontextEnv`（短期上下文），并提供格式奖励和 LLM-as-a-Judge 等多维奖励信号。
*   **Trainer Layer（训练层）**：核心优化引擎。
    *   **关键算法**：原生集成 **GRPO（Group Relative Policy Optimization）**。
    *   **优势**：GRPO 通过组内奖励归一化估计基准，无需额外的 Critic 网络，极大地降低了长上下文训练时的显存开销。

### 3. 实验设计
*   **实验对象**：基于 MemAgent 架构构建的智能体。
*   **基座模型**：Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集/场景**：
    *   使用 MemAgent 公开的训练集进行微调。
    *   **Benchmark**：包含两个主任务测试集（`eval_50`, `eval_100`）和一个分布外（OOD）测试集（`eval_fwe_16384`）。
*   **对比基准**：对比了基座模型（Base Checkpoint）与经过 MemFactory RL 优化后的模型性能。

### 4. 资源与算力
*   **硬件环境**：实验明确指出整个训练和评估流水线可以在 **单张 NVIDIA A800 80GB GPU** 上执行。
*   **训练细节**：每个模型训练 250 个有效步（valid steps）。为了提高效率，研究者对训练样本进行了简化，将上下文长度缩减至原数据的约 1/3（每样本 50-80 个文档）。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同参数规模的模型进行了完整训练与评估。
*   **客观性保障**：所有评分均基于 **4 次独立试验的平均值（avg@4）**，以消除 LLM 生成随机性带来的偏差。
*   **充分性**：实验不仅覆盖了主任务，还包含了 OOD 任务以验证策略的泛化性。虽然实验主要聚焦于 MemAgent 这一种范式，但其结果足以证明框架在 RL 优化方面的有效性。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **泛化能力**：较大规模的模型（4B）在 OOD 任务上表现出更强的鲁棒性，能够将学到的递归记忆策略有效迁移至未见场景。
*   **可行性验证**：证明了在有限算力条件下，通过 GRPO 优化复杂的长上下文记忆策略是切实可行的。

### 7. 优点与亮点
*   **统一性与标准化**：首个将记忆增强智能体的推理与训练统一化的框架，降低了研究门槛。
*   **高度可扩展**：模块化设计允许研究者像搭积木一样更换检索器或更新器。
*   **显存高效**：原生支持 GRPO，解决了记忆增强任务中上下文窗口饱和导致的显存瓶颈。
*   **开箱即用**：内置了对 Memory-R1、RMM 等前沿算法的支持。

### 8. 不足与局限
*   **范式覆盖有限**：虽然框架支持多种范式，但目前的实证研究主要集中在 MemAgent 上，对其他范式（如 Memory-R1 的 CRUD 操作）的深度对比尚不充分。
*   **训练效率**：尽管支持单卡训练，但在处理极长序列时，训练吞吐量仍有提升空间。
*   **任务多样性**：实验数据集相对单一，未来需在更多样化的智能体交互场景（如多智能体协作、复杂工具调用）中验证其通用性。

（完）
