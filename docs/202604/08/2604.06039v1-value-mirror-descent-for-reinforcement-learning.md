---
title: Value Mirror Descent for Reinforcement Learning
title_zh: 强化学习中的价值镜像下降
authors: "Zhichao Jia, Guanghui Lan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06039v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 结合镜像下降的价值迭代框架
tldr: 本文提出了一种名为价值镜像下降（VMD）的新型强化学习算法，将凸优化的镜像下降思想融入经典的价值迭代框架中。针对生成模型下的随机设置，作者开发了结合方差缩减技术的SVMD算法。该方法在处理一般凸正则化项时达到了近乎最优的样本复杂度，并确保了生成策略与最优策略间的Bregman散度有界，为离线训练后的在线持续学习提供了理论保障。
source: arxiv
selection_source: fresh_fetch
motivation: 旨在结合价值迭代的高样本效率与镜像下降的优化优势，解决现有随机价值迭代方法在策略稳定性及在线学习衔接方面的不足。
method: 提出价值镜像下降（VMD）及其随机变体（SVMD），通过在价值迭代中引入镜像下降和方差缩减技术来优化策略更新过程。
result: 在一般凸正则化下实现了近优的样本复杂度，并在强凸正则化下进一步提升了高精度区域的性能，同时保证了策略间的散度有界。
conclusion: 该研究为强化学习提供了具有严谨理论保证的新型优化框架，在提升样本效率和保证策略收敛性方面做出了重要贡献。
---

## 摘要
价值迭代类方法在强化学习（RL）中为计算近优价值函数已被广泛研究。在生成式采样模型下，这些方法可以实现比策略优化方法更优的样本复杂度，特别是在对折扣因子的依赖方面。在实践中，它们通常被用于离线训练或模拟环境。在本文中，我们考虑具有状态空间 S、动作空间 A、折扣因子 $γ\in(0,1)$ 且代价在 $[0,1]$ 范围内的折扣马尔可夫决策过程。我们提出了一种新颖的价值优化方法，称为价值镜像下降（VMD），它将凸优化中的镜像下降集成到经典的价值迭代框架中。在转移核已知的确定性设置下，我们证明了 VMD 线性收敛。对于具有生成模型的随机设置，我们开发了一个随机变体 SVMD，它结合了随机价值迭代类方法中常用的方差缩减技术。对于具有一般凸正则化项的 RL 问题，SVMD 达到了 $\tilde{O}(|S||A|(1-γ)^{-3}ε^{-2})$ 的近优样本复杂度。此外，我们证明了在整个迭代过程中，生成的策略与最优策略之间的 Bregman 散度保持有界。这一性质在现有的随机价值迭代类方法中是不存在的，但对于在离线训练后实现有效的在线（持续）学习至关重要。在强凸正则化项下，SVMD 实现了 $\tilde{O}(|S||A|(1-γ)^{-5}ε^{-1})$ 的样本复杂度，提高了在高精度区域的性能。此外，我们证明了生成的策略收敛于最优策略。总之，所提出的方法、其分析以及由此产生的保证，为 RL 和优化领域做出了新的贡献。

## Abstract
Value iteration-type methods have been extensively studied for computing a nearly optimal value function in reinforcement learning (RL). Under a generative sampling model, these methods can achieve sharper sample complexity than policy optimization approaches, particularly in their dependence on the discount factor. In practice, they are often employed for offline training or in simulated environments. In this paper, we consider discounted Markov decision processes with state space S, action space A, discount factor $γ\in(0,1)$ and costs in $[0,1]$. We introduce a novel value optimization method, termed value mirror descent (VMD), which integrates mirror descent from convex optimization into the classical value iteration framework. In the deterministic setting with known transition kernels, we show that VMD converges linearly. For the stochastic setting with a generative model, we develop a stochastic variant, SVMD, which incorporates variance reduction commonly used in stochastic value iteration-type methods. For RL problems with general convex regularizers, SVMD attains a near-optimal sample complexity of $\tilde{O}(|S||A|(1-γ)^{-3}ε^{-2})$. Moreover, we establish that the Bregman divergence between the generated and optimal policies remains bounded throughout the iterations. This property is absent in existing stochastic value iteration-type methods but is important for enabling effective online (continual) learning following offline training. Under a strongly convex regularizer, SVMD achieves sample complexity of $\tilde{O}(|S||A|(1-γ)^{-5}ε^{-1})$, improving performance in the high-accuracy regime. Furthermore, we prove convergence of the generated policy to the optimal policy. Overall, the proposed method, its analysis, and the resulting guarantees, constitute new contributions to the RL and optimization literature.

---

## 论文详细总结（自动生成）

这篇论文由佐治亚理工学院的 Zhichao Jia 和 Guanghui Lan 撰写，提出了一种结合镜像下降（Mirror Descent）与价值迭代（Value Iteration）的新型强化学习框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在强化学习中，**价值迭代类方法**（如 Q-Learning）在生成模型下具有极高的样本效率（对折扣因子 $\gamma$ 的依赖更优），但存在策略不稳定、难以处理正则化项以及在强凸正则化下无法达到 $O(\epsilon^{-1})$ 样本复杂度等问题。相比之下，**策略优化方法**（如 PPO、镜像下降）虽然稳定且易于处理正则化，但在样本复杂度上通常较差。
**研究动机：** 论文旨在弥合这两类方法的鸿沟，提出一种既能保持价值迭代的高样本效率，又能具备镜像下降的稳定性和正则化处理能力的算法框架，特别是解决离线预训练策略在后续在线微调时的稳定性问题。

### 2. 论文提出的方法论：核心思想与关键技术
论文提出了 **价值镜像下降（Value Mirror Descent, VMD）** 及其随机版本 **SVMD**：
*   **核心思想：** 在经典的价值迭代步骤中引入“近端映射”（Prox-mapping）。算法不再是简单地贪婪更新价值函数，而是交替进行策略改进和价值更新。
*   **关键技术细节：**
    *   **近端映射步骤：** 通过最小化包含当前代价、未来价值估计、凸正则化项 $h(\pi)$ 以及与前一策略的 **Bregman 散度** 的目标函数来更新策略 $\pi_{k+1}$。
    *   **价值更新步骤：** 利用新策略更新价值向量 $V_{k+1}$。
    *   **方差缩减（Variance Reduction）：** 在随机设置（SVMD）中，利用生成模型采样，通过估计价值变化的差值（而非直接估计全量价值）来降低采样噪声。
    *   **单调性维护：** 引入了组件级的“取小”操作（min operator），确保在存在随机噪声的情况下，价值函数序列仍能保持近似单调递减，从而保证收敛。

### 3. 实验设计
*   **性质说明：** 本论文是一篇**纯理论证明型**的学术论文。
*   **场景与 Benchmark：** 论文并未在具体的仿真环境（如 Gym 或 Atari）中运行代码实验，而是基于**折扣马尔可夫决策过程（DMDP）**的数学模型进行理论推导。
*   **对比对象：** 论文在理论层面对比了经典的价值迭代（VI）、方差缩减 Q 迭代（Variance-reduced Q-VI）以及策略镜像下降（PMD）的**样本复杂度（Sample Complexity）**和**收敛速率**。

### 4. 资源与算力
*   **算力说明：** 论文中**未提及**任何 GPU/CPU 算力消耗、训练时长或硬件型号。
*   **原因：** 所有的结论均通过数学引理和定理证明得出，不涉及大规模数值计算或深度学习训练。

### 5. 实验数量与充分性
*   **理论充分性：** 论文提供了长达 27 页的详细数学证明，涵盖了确定性环境、一般凸正则化随机环境以及强凸正则化随机环境三种场景。
*   **客观性：** 证明过程严谨，给出了明确的收敛界限和样本复杂度上界，并与已知的理论下界（Lower Bound）进行了对比，证明了其算法在一般凸设置下的近乎最优性（Near-optimality）。

### 6. 论文的主要结论与发现
*   **线性收敛：** 在确定性设置下，VMD 具有线性收敛速度，迭代复杂度为 $\tilde{O}((1-\gamma)^{-1})$。
*   **一般凸正则化：** SVMD 达到了 $\tilde{O}(|S||A|(1-\gamma)^{-3}\epsilon^{-2})$ 的样本复杂度，这在对 $\epsilon$ 的依赖上是近乎最优的。
*   **强凸正则化突破：** 在强凸正则化下，SVMD 实现了 $\tilde{O}(\epsilon^{-1})$ 的样本复杂度。这是**首个**达到该复杂度的价值迭代类方法，显著优于传统方法的 $O(\epsilon^{-2})$。
*   **策略稳定性：** 证明了生成的策略与最优策略之间的 Bregman 散度是有界的。这意味着该算法产生的策略非常“稳”，适合作为在线持续学习的预训练模型。

### 7. 优点
*   **理论突破：** 首次将镜像下降的优化视角成功融入价值迭代框架，并给出了严谨的收敛性分析。
*   **高精度性能：** 在强凸正则化下提升了样本复杂度对 $\epsilon$ 的依赖，使其在高精度需求场景下更具优势。
*   **衔接性强：** 解决了价值迭代输出策略不稳定的痛点，为“离线预训练+在线微调”的工业流程提供了理论支撑。

### 8. 不足与局限
*   **缺乏实证：** 论文完全依赖理论分析，没有在复杂的高维连续空间（如深度强化学习任务）中验证其算法的实际表现。
*   **参数敏感性：** 理论上的样本量要求（如 $m_{k,1}, m_{k,2}$）通常非常保守，在实际应用中可能需要大量的超参数调优。
*   **复杂度代价：** 虽然对 $\epsilon$ 的依赖优化了，但在强凸设置下，对 $(1-\gamma)$ 的依赖（$-5$ 次方）比一般凸设置（$-3$ 次方）更差，这在极高折扣因子的任务中可能成为瓶颈。

（完）
