---
title: "Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies"
title_zh: 学习在测试时学习：具有可学习自适应策略的语言智能体
authors: "Zhanzhi Lou, Hui Chen, Yibo Li, Qian Wang, Bryan Hooi"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00830v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 具有可学习自适应策略的语言智能体
tldr: 本研究针对语言智能体在推理阶段通过交互提升性能的测试时学习（TTL）展开。针对现有方法依赖人工设计适应策略的局限，提出了 Meta-TTL 框架。该框架将适应策略的发现建模为双层优化问题：内层执行 TTL 过程，外层利用进化搜索在多样化任务上迭代优化策略。实验表明，Meta-TTL 在 Jericho 和 WebArena-Lite 任务中表现优异，证明了学习到的适应策略具有强大的泛化能力和跨任务迁移性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00830v1/fig-001.webp\", \"caption\": \"Table 1: Jericho ID Results. Comparison of single-agent baselines and actor+meta-agent methods on meta-training games. Single-agent baselines are shown at the top for reference; within each meta-agent block, META-TTL improves overall performance over the Naive variant.\", \"page\": 7, \"index\": 1, \"width\": 822, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2604.00830v1/fig-002.webp\", \"caption\": \"Table 2: Jericho OOD Results. Zero-shot generalization on held-out games, with singleagent baselines shown at the top for reference. META-TTL generally improves performance across all three meta-agent backbones.\", \"page\": 7, \"index\": 2, \"width\": 822, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2604.00830v1/fig-003.webp\", \"caption\": \"Table 4: WebArena-Lite OOD Results. Zero-shot generalization on out-of-distribution domains, with single-agent baselines shown at the top for reference. META-TTL improves OOD average performance for all meta-model backbones.\", \"page\": 8, \"index\": 3, \"width\": 822, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.00830v1/fig-004.webp\", \"caption\": \"Table 3: WebArena-Lite ID Results. Comparison of single-agent baselines and actor+metaagent methods on in-distribution domains. Within each meta-agent block, META-TTL improves average performance across all meta-agent models.\", \"page\": 8, \"index\": 4, \"width\": 820, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2604.00830v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of META-TTL. Outer loop (meta-training): A proposer LM reflects and proposes candidate meta-prompts, which are validated locally and globally before entering a per-task expert pool. After training, a single optimized meta-prompt ϕ∗ is selected from this pool. Inner loop (test-time learning): The meta-agent, governed by ϕ∗, observes the actor’s trajectory after each episode and generates verbal feedback that rewrites the actor’s system prompt for the next attempt.\", \"page\": 4, \"index\": 5, \"width\": 822, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2604.00830v1/fig-006.webp\", \"caption\": \"Figure 3: Per-episode score trajectories on the six Jericho evaluation games. META-TTL exhibits clearer upward trends across episodes than the baselines, supporting W-AUC as a metric of sustained test-time improvement.\", \"page\": 9, \"index\": 6, \"width\": 824, \"height\": 406}]"
motivation: 现有的测试时学习方法主要依赖人工设计的固定适应策略，而非针对下游任务改进进行优化。
method: 提出 Meta-TTL 框架，通过双层优化和进化搜索，在多样化的训练任务中自动学习并迭代优化适应策略。
result: 在 Jericho 和 WebArena-Lite 基准测试中，Meta-TTL 在分布内和分布外设置下均显著优于人工设计的基准方法。
conclusion: 研究证明了通过学习获得的适应策略能够编码可迁移的知识，有效提升语言智能体在未知任务中的泛化表现。
---

## 摘要
测试时学习（TTL）使语言智能体能够在推理阶段通过与环境的重复交互来迭代优化其性能。TTL 的核心是一个自适应策略，它根据先前回合的经验更新执行者策略（actor policy），从而改善未来的行为。现有方法依赖于固定的、手工设计的自适应策略，而不是针对下游改进对其进行优化。我们认为，最优的自适应策略应该从任务环境中学习，而不是基于人类直觉进行手工设计。为此，我们引入了 Meta-TTL，这是一个将有效自适应策略的发现过程建模为双层优化问题的框架。在该框架内，内循环执行标准的 TTL 过程，衡量候选自适应策略在连续回合中帮助智能体纠正错误的有效性。在智能体性能的指导下，外循环在多样化的训练任务分布上采用进化搜索，以迭代优化自适应策略。我们在 Jericho 和 WebArena-Lite 上评估了 Meta-TTL，涵盖了分布内（ID）和分布外（OOD）设置，并使用了多种元智能体骨干网络。两个基准测试的结果均显示，Meta-TTL 一致优于手工设计的基准方法，这表明优化后的自适应策略编码了可迁移的策略，能够泛化到训练任务分布之外。

## Abstract
Test-Time Learning (TTL) enables language agents to iteratively refine their performance through repeated interactions with the environment at inference time. At the core of TTL is an adaptation policy that updates the actor policy based on experience from previous episodes, thereby improving future behavior. Existing methods rely on fixed, hand-crafted adaptation policies rather than optimizing them for downstream improvement. We argue that optimal adaptation policies should be learned from task environments, not hand-engineered based on human intuition. To achieve this, we introduce Meta-TTL, a framework that formulates the discovery of effective adaptation policies as a bi-level optimization problem. Within this framework, the inner loop executes the standard TTL process, measuring how effectively a candidate adaptation policy helps an agent correct errors across sequential episodes. Guided by the agent's performance, the outer loop employs evolutionary search over a diverse distribution of training tasks to iteratively refine the adaptation policy. We evaluate Meta-TTL on Jericho and WebArena-Lite across both in-distribution (ID) and out-of-distribution (OOD) settings, using multiple meta-agent backbones. Results on both benchmarks show that Meta-TTL consistently outperforms hand-crafted baselines, suggesting that the optimized adaptation policy encodes transferable strategies that generalize beyond the training task distribution.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管大语言模型（LLM）智能体具备强大的零样本能力，但在面对陌生环境时，往往无法像人类一样通过“失败-诊断-调整-重试”的循环进行有效进化。
*   **核心问题**：现有的“测试时学习”（Test-Time Learning, TTL）方法（如 Reflexion）大多依赖于人工设计的、固定的反馈逻辑（即自适应策略）。这些策略基于人类直觉而非针对任务优化，导致智能体在重复尝试中往往重复同样的错误。
*   **整体含义**：本研究提出将“如何学习”本身视为一个可优化的能力。通过从任务环境中学习自适应策略，使智能体能够自动发现更有效的自我改进规则，从而在推理阶段实现更显著的性能提升。

### 2. 方法论：Meta-TTL 框架
*   **核心思想**：将 TTL 建模为一个**双层优化（Bi-level Optimization）**问题。
    *   **内循环（Inner Loop）**：执行标准的测试时学习。执行者（Actor）在 $K$ 个回合中尝试任务，元智能体（Meta-Agent）观察轨迹并重写执行者的系统提示词（System Prompt）以改进下一回合的表现。
    *   **外循环（Outer Loop）**：执行进化元训练。在训练任务分布上，通过进化搜索（Evolutionary Search）不断迭代优化元智能体的“元提示词”（Meta-Prompt $\phi$）。
*   **关键技术细节**：
    *   **评价指标（W-AUC）**：引入加权学习曲线下面积（Weighted Area Under the Learning Curve），给后期回合更高的权重，以奖励那些能带来持续改进的策略。
    *   **进化算子**：使用一个 Proposer LLM 对当前的元提示词进行反思并提出改进建议（变异），通过局部验证（单个任务）和全局验证（验证集全集）来筛选优秀的候选策略。
    *   **专家池（Expert Pool）**：为每个验证任务维护最佳策略，最终选择平均表现（或标准化后的 Z-Score）最高的策略作为最终输出。

### 3. 实验设计
*   **数据集/场景**：
    *   **Jericho**：交互式小说游戏（文本冒险），具有密集的得分奖励。
    *   **WebArena-Lite**：网页导航任务，具有稀疏的二元奖励（成功/失败）。
*   **Benchmark 设置**：
    *   **分布内（ID）**：在训练过的游戏/网站域上评估。
    *   **分布外（OOD）**：在完全未见过的游戏/网站域上评估，验证策略的迁移性。
*   **对比方法**：
    *   **Static**：无适应过程。
    *   **Reflexion**：经典的人工设计语言反馈方法。
    *   **Memory Agent**：带有进化记忆机制的智能体。
    *   **Naive Meta-Agent**：使用未经过元训练的初始元提示词。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号和数量（由于主要基于 LLM API 调用）。
*   **训练时长**：文中提到一个典型的实验案例：使用 GPT-5 作为元智能体在 Jericho ID 任务上运行了 **26 次迭代**，总耗时约 **27 小时**。
*   **模型骨干**：实验使用了 Gemini 3 Flash、GLM-5 和 GPT-5（注：根据论文标注的 2026 年发布时间，这些为该时间点的先进模型）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个截然不同的任务领域（文本游戏 vs 网页导航），使用了三种不同的顶级 LLM 作为元智能体骨干，进行了 ID 和 OOD 的全面对比。
*   **充分性评价**：实验设计较为充分。通过对比 Naive 变体，证明了元训练的必要性；通过 OOD 测试，证明了学习到的不是简单的任务知识，而是通用的“学习算法”；通过消融实验展示了 Z-Score 标准化在专家选择中的重要性。

### 6. 主要结论与发现
*   **性能飞跃**：Meta-TTL 在 Jericho ID 任务上将平均得分从 50.4 提升至 110.8（约 120% 提升），在 WebArena 上也有显著增长。
*   **泛化能力**：学习到的自适应策略在 OOD 任务上依然有效，证明了“学习如何学习”的能力是可以跨环境迁移的。
*   **策略涌现**：优化后的元提示词自动演化出了高级策略，包括：**明确的信用分配**（识别哪些动作得分）、**受控探索**（每次只尝试一个新分支）、**条件化知识库**（仅在确认环境后激活特定知识）。

### 7. 优点
*   **范式创新**：将自适应逻辑从“人工设计”转向“自动学习”，填补了 TTL 领域在策略优化上的空白。
*   **可解释性**：最终产出的是自然语言形式的元提示词，人类可以直观地理解智能体学到了哪些改进技巧。
*   **轻量化部署**：测试时无需更新模型权重，仅通过提示词重写实现，具有很高的工程实用性。

### 8. 不足与局限
*   **搜索成本高**：进化搜索过程依赖大量 LLM 调用，元训练阶段的 API 开销和时间成本较高。
*   **奖励敏感性**：在奖励极其稀疏的任务（如 WebArena）中，由于反馈信号弱，元训练的优化难度显著增加，提升幅度不如密集奖励任务。
*   **依赖 Proposer 能力**：框架的效果高度依赖于外循环中 Proposer LLM 的反思和提议能力，若基础模型能力不足，可能无法发现高级策略。

（完）
