---
title: Asymmetric Actor-Critic for Multi-turn LLM Agents
title_zh: 面向多轮大语言模型智能体的不对称 Actor-Critic 框架
authors: "Shuli Jiang, Zhaoyang Zhang, Yi Zhang, Shuo Yang, Wei Xia, Stefano Soatto"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00304v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 针对大语言模型智能体的不对称演员-评论家框架
tldr: 针对多轮对话智能体在单次尝试中可靠性不足的问题，本文提出一种非对称 Actor-Critic 框架。该框架利用强大的闭源模型作为 Actor，配合轻量级开源模型作为 Critic 进行实时监控与干预。通过利用生成与验证之间的不对称性，该方法在无需修改 Actor 的情况下，通过微调 Critic 显著提升了智能体在复杂任务中的成功率和可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00304v1/fig-001.webp\", \"caption\": \"Figure 1: Existing actor–critic methods assume trainable actors and structured action spaces. We propose an asymmetric actor–critic framework where a lightweight open-source critic supervises a fixed proprietary actor at runtime, enabling reliable control for one-shot multiturn conversational agents.\", \"page\": 1, \"index\": 1, \"width\": 458, \"height\": 291}, {\"url\": \"assets/figures/arxiv/2604.00304v1/fig-002.webp\", \"caption\": \"Table 13: Statistics of the generated critic training data. Each critic evaluation point in a trajectory corresponds to one training sample. Positive samples denote critic rejections, while negative samples denote approvals. For UserBench, the critic is trained on data aggregated across all environments.\", \"page\": 18, \"index\": 2, \"width\": 779, \"height\": 204}]"
motivation: 现有的智能体优化方法通常依赖多次尝试或全量微调，难以满足闭源大模型在不可重试的实时多轮交互场景下的可靠性需求。
method: 采用非对称架构，由高性能闭源模型担任 Actor，轻量级开源模型担任 Critic 进行实时监督干预，并设计了专门的 Critic 微调数据生成流水线。
result: 在 τ-bench 和 UserBench 实验中，该框架显著提升了任务成功率，且微调后的轻量级 Critic 表现优于或等同于大型闭源模型。
conclusion: 研究证明了通过小型模型实时监督大型闭源模型的可行性，为构建可靠的多轮对话智能体提供了一种高效且经济的新范式。
---

## 摘要
大语言模型（LLM）展现出强大的推理和对话能力，但在多轮交互中确保其行为的可靠性仍然具有挑战性。在许多现实应用中，智能体必须在无法重试的单次尝试（one-shot）场景下取得成功。现有方法要么依赖于反思或事后评估（这需要额外的尝试），要么假设模型是完全可训练的，从而无法利用闭源 LLM。我们提出了一种用于构建可靠对话智能体的不对称 Actor-Critic 框架。其中，强大的闭源 LLM 充当 Actor（执行者），而较小的开源 Critic（评论者）提供运行时监督，监控 Actor 的动作并在同一交互轨迹内进行干预。与基于训练的 Actor-Critic 方法不同，我们的框架监督的是在开放式对话环境中运行的固定 Actor。该设计利用了生成与验证之间的不对称性：虽然高质量的生成需要大模型，但有效的监督通常可以通过较小的模型来实现。我们还引入了一种数据生成流水线，在不修改 Actor 的情况下生成用于 Critic 微调的监督信号。在 $τ$-bench 和 UserBench 上的实验表明，与强大的单智能体基准相比，我们的方法显著提高了可靠性和任务成功率。此外，轻量级开源 Critic 在担任评论者角色时可以媲美甚至超越更大的闭源模型，且 Critic 微调在多种最先进方法的基础上带来了额外的增益。

## Abstract
Large language models (LLMs) exhibit strong reasoning and conversational abilities, but ensuring reliable behavior in multi-turn interactions remains challenging. In many real-world applications, agents must succeed in one-shot settings where retries are impossible. Existing approaches either rely on reflection or post-hoc evaluation, which require additional attempts, or assume fully trainable models that cannot leverage proprietary LLMs. We propose an asymmetric actor-critic framework for reliable conversational agents. A powerful proprietary LLM acts as the actor, while a smaller open-source critic provides runtime supervision, monitoring the actor's actions and intervening within the same interaction trajectory. Unlike training-based actor-critic methods, our framework supervises a fixed actor operating in open-ended conversational environments. The design leverages a generation-verification asymmetry: while high-quality generation requires large models, effective oversight can often be achieved by smaller ones. We further introduce a data generation pipeline that produces supervision signals for critic fine-tuning without modifying the actor. Experiments on $τ$-bench and UserBench show that our approach significantly improves reliability and task success over strong single-agent baselines. Moreover, lightweight open-source critics rival or surpass larger proprietary models in the critic role, and critic fine-tuning yields additional gains over several state-of-the-art methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Asymmetric Actor-Critic for Multi-turn LLM Agents》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**多轮对话智能体在现实应用中的可靠性问题**。
*   **研究动机**：虽然 LLM 具备强大的生成能力，但在处理复杂业务规则、领域约束和用户偏好时，常会出现幻觉、违反政策或推理错误。
*   **核心挑战**：在许多实际场景（如客户服务）中，智能体必须在“单次尝试（One-shot）”中成功，无法进行多次重试或事后修正。
*   **现有局限**：现有的反思（Reflection）方法通常需要多次尝试；而传统的 Actor-Critic 强化学习框架通常假设 Actor 是可训练的开源模型，且动作空间是结构化的，这不适用于目前主流的闭源 API 模型（如 Claude、GPT-4）以及开放式的自然语言对话环境。

### 2. 方法论：不对称 Actor-Critic 框架
论文提出了一种**不对称 Actor-Critic（Asym-AC）**架构，核心思想是利用“生成与验证的不对称性”。
*   **核心组件**：
    *   **Actor（执行者）**：使用强大的固定闭源模型（如 Claude-3.5 Sonnet），负责对话生成、工具调用和决策。
    *   **Critic（评论者）**：使用轻量级开源模型（如 Qwen3-8B），负责实时监控 Actor 的动作。
*   **运行时监督流程**：
    1. Actor 提出一个候选动作（如回复或工具调用）。
    2. Critic 评估该动作是否违反约束（政策违规、幻觉等）。
    3. 若发现问题，Critic 提供自然语言反馈，干预并要求 Actor 在同一轮次内修正动作。
*   **Critic 训练流水线**：
    *   **自博弈数据生成**：利用现有对话数据集，通过 Actor 重新运行任务，识别 Actor 失败的“困难任务”。
    *   **干预信号提取**：在 Actor-Critic 协同成功的轨迹中，提取 Critic 的干预决策和反馈内容，构建监督微调（SFT）数据集。

### 3. 实验设计
*   **数据集/场景**：
    *   **$\tau$-bench**：模拟零售和航空领域的客户服务，涉及复杂的业务政策和工具调用。
    *   **UserBench**：模拟旅游规划，要求智能体通过多轮对话推断用户隐含偏好并提供最优推荐。
*   **对比基准（Baselines）**：
    *   **Single Actor**：仅使用闭源模型（Claude-4 Sonnet）的基准。
    *   **CRITIC**：基于提示词的自我修正方法。
    *   **ReAct**：经典的推理-动作交替框架。
    *   **APIGen-MT & IRMA**：针对 $\tau$-bench 的特定优化方法。
*   **消融与对比**：对比了不同规模的 Critic（4B, 8B, 32B）以及闭源模型作为 Critic 的效果。

### 4. 资源与算力
*   **训练细节**：使用了 TRL 框架进行微调，采用 LoRA 技术（Rank=16, Alpha=32）。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到 Critic 模型规模较小（4B-32B），且微调轮数为 5-15 轮，暗示其算力需求远低于全量微调大型模型。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准的多个子领域（零售、航空、T-22/33/44 旅游规划）进行了测试。
*   **充分性**：
    *   每个任务均进行了 **5 次独立运行**并取平均值（Pass@1），保证了结果的统计显著性。
    *   涵盖了从“无微调 Critic”到“微调后 Critic”的性能演进对比。
    *   进行了 Critic 模型容量的对比实验，验证了“小模型监督大模型”的有效性。
*   **客观性**：对比了多种 SOTA 方法，并提供了详细的定性案例分析，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能提升**：Asym-AC 框架在所有基准测试中均显著优于单智能体基准。
*   **不对称优势**：轻量级开源 Critic（如 Qwen3-8B/32B）在监督任务中的表现**媲美甚至超过**了规模大得多的闭源模型（如 Claude-4 Sonnet），证明了验证任务对模型规模的依赖较低。
*   **微调增益**：通过论文提出的数据流水线微调后的 Critic（Asym-AC-SFT）带来了进一步的性能飞跃。

### 7. 优点
*   **实用性强**：无需修改闭源 Actor 的参数，非常适合基于 API 开发的商业应用。
*   **低成本高效**：利用小模型作为“安全护栏”，在不显著增加延迟的情况下提升了可靠性。
*   **单次成功率**：专注于在同一交互轨迹内修正错误，满足了实时交互场景的需求。

### 8. 不足与局限
*   **延迟增加**：由于每轮关键决策都需要 Critic 介入，会带来额外的推理延迟和计算成本。
*   **Actor 依赖性**：如果 Actor 本身的推理能力极差，即使 Critic 指出错误，Actor 可能也无法根据反馈做出正确修正。
*   **干预时机**：目前的干预逻辑（$\delta_t$）在某些场景下可能需要人工预设触发条件（如仅在调用工具时触发），自动化触发机制仍有优化空间。

（完）
