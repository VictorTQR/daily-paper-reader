---
title: "CLEAR: Context Augmentation from Contrastive Learning of Experience via Agentic Reflection"
title_zh: CLEAR：通过智能体反思进行经验对比学习的上下文增强
authors: "Linbo Liu, Guande Wu, Han Ding, Yawei Wang, Qiang Zhou, Yuzhe Lu, Zhichao Xu, Huan Song, Panpan Xu, Lin Lee Cheong"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07487v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 生成式上下文增强与过去经验检索
tldr: 本文提出CLEAR框架，旨在解决LLM智能体在决策时依赖检索旧经验而带来的推理负担。该框架通过反思智能体对历史执行轨迹进行对比分析并生成知识摘要，随后利用监督微调和强化学习训练上下文增强模型（CAM）。相比于传统的检索机制，CLEAR能为当前任务生成更具针对性的定制化背景信息，显著提升了智能体在复杂环境下的任务处理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07487v1/fig-001.webp\", \"caption\": \"Figure 2: During inference, a new task qnew is sampled from Dtest and is passed into πC θ to generate cnew ∼ πC θ (qnew). The auxiliary context cnew is appended to qnew and the execution agent πE starts with qnew ⊕ cnew.\", \"page\": 6, \"index\": 1, \"width\": 706, \"height\": 194}, {\"url\": \"assets/figures/arxiv/2604.07487v1/fig-002.webp\", \"caption\": \"Table 1: AppWorld experiments results. Task Goal Completion (TGC) and Scenario Goal Completion (SGC) on the TEST-N split are reported. Results are averaged over three runs (standard deviation in parentheses) except the Pass@3 metric.\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.07487v1/fig-003.webp\", \"caption\": \"Table 5: AppWorld experiments results. Task Goal Completion (TGC) and Scenario Goal Completion (SGC) on the TEST-N split are reported. Results are averaged over three runs (standard deviation in parentheses) except the Pass@3 metric.\", \"page\": 17, \"index\": 3, \"width\": 826, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.07487v1/fig-004.webp\", \"caption\": \"Table 2: WebShop-40k experiments results. Averaged reward on the test dataset is reported. Results are averaged over three runs with standard deviation shown in parentheses.\", \"page\": 9, \"index\": 4, \"width\": 826, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2604.07487v1/fig-005.webp\", \"caption\": \"Figure 1: CLEAR training framework design. First, we execute each task qi ∼ Dtrain for m times and collect groups of replay Γi for qi. We employ reflection agents πR to generate instance-level instruction ci for each qi, collected into DSFT. Then, we initialize CAM from an open-source LLM and perform SFT on DSFT. Finally, we further perform RL on the trained CAM, which leverages the reward signal from πE for policy update of CAM.\", \"page\": 5, \"index\": 5, \"width\": 748, \"height\": 423}, {\"url\": \"assets/figures/arxiv/2604.07487v1/fig-006.webp\", \"caption\": \"Table 3: Ablation study on AppWorld TEST-N split. For the CAM, we use the following variant: (1) a Qwen/Qwen3-32B model without SFT and RL as in experiment 1. (2) a Qwen332B model after SFT on DSFT no CL (no RL) as in experiment 2. (3) a Qwen3-32B model after SFT with CL (no RL) as in experiment 3. (4) full CLEAR framework as in experiment 4. Results are averaged over three runs (standard deviation in parentheses) except the Pass@3 metric.\", \"page\": 16, \"index\": 6, \"width\": 826, \"height\": 286}]"
motivation: 现有的检索式上下文工程要求智能体自行适配旧经验，增加了推理负担，因此需要一种能直接生成定制化任务知识的方法。
method: 通过反思智能体对比分析历史轨迹生成摘要，并结合监督微调与基于任务执行奖励的强化学习来优化上下文增强模型。
result: "在AppWorld测试集上将任务完成率从72.62%提升至81.15%，在WebShop子集上将平均奖励从0.68提升至0.74。"
conclusion: CLEAR通过学习生成任务特定的知识而非简单检索，有效减轻了智能体的推理压力，显著增强了LLM智能体在复杂基准测试中的表现。
---

## 摘要
大语言模型智能体依赖有效的模型上下文来获取决策所需的任务相关信息。许多现有的上下文工程方法主要依赖于从过去经验中生成的上下文，以及重用这些上下文的检索机制。然而，从过去任务中检索到的上下文必须由执行智能体进行调整以适应新情况，这给底层大语言模型带来了额外的推理负担。为了解决这一局限性，我们提出了一种生成式上下文增强框架，即通过智能体反思进行经验对比学习（CLEAR）。CLEAR 首先利用反思智能体对过去的执行轨迹进行对比分析，并为每个观察到的任务总结有用的上下文。这些总结随后被用作监督微调数据，以训练上下文增强模型（CAM）。随后，我们利用强化学习进一步优化 CAM，其中奖励信号通过运行任务执行智能体获得。通过学习生成特定于任务的知识而非从过去检索知识，CAM 产生的上下文能更好地适配当前任务。我们在 AppWorld 和 WebShop 基准测试上进行了全面评估。实验结果表明，CLEAR 一贯优于强基准模型。与基准智能体相比，它在 AppWorld 测试集上的任务完成率从 72.62% 提高到 81.15%，在 WebShop 子集上的平均奖励从 0.68 提高到 0.74。我们的代码已在 https://github.com/awslabs/CLEAR 公开。

## Abstract
Large language model agents rely on effective model context to obtain task-relevant information for decision-making. Many existing context engineering approaches primarily rely on the context generated from the past experience and retrieval mechanisms that reuse these context. However, retrieved context from past tasks must be adapted by the execution agent to fit new situations, placing additional reasoning burden on the underlying LLM. To address this limitation, we propose a generative context augmentation framework using Contrastive Learning of Experience via Agentic Reflection (CLEAR). CLEAR first employs a reflection agent to perform contrastive analysis over past execution trajectories and summarize useful context for each observed task. These summaries are then used as supervised fine-tuning data to train a context augmentation model (CAM). Then we further optimize CAM using reinforcement learning, where the reward signal is obtained by running the task execution agent. By learning to generate task-specific knowledge rather than retrieve knowledge from the past, CAM produces context that is better tailored to the current task. We conduct comprehensive evaluations on the AppWorld and WebShop benchmarks. Experimental results show that CLEAR consistently outperforms strong baselines. It improves task completion rate from 72.62% to 81.15% on AppWorld test set and averaged reward from 0.68 to 0.74 on a subset of WebShop, compared with baseline agent. Our code is publicly available at https://github.com/awslabs/CLEAR.

---

## 论文详细总结（自动生成）

### 论文总结：CLEAR —— 通过智能体反思进行经验对比学习的上下文增强

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）智能体在处理复杂、知识密集型任务时，往往面临参数化知识陈旧或不足的问题。现有的解决方案如**检索增强生成（RAG）**或**提示优化（如 ACE）**主要依赖于从过去经验中检索相似片段。
*   **痛点**：检索到的旧经验往往不能直接适配新任务，要求执行智能体（Execution Agent）在推理过程中自行进行二次适配，这显著增加了底层 LLM 的推理负担，尤其是在模型能力有限或任务差异较大时。
*   **核心目标**：提出一种**生成式**上下文增强框架，通过训练一个专门的“上下文增强模型（CAM）”，直接为当前任务生成定制化的、可操作的指导信息，从而减轻执行智能体的压力。

#### 2. 方法论：核心思想与技术细节
CLEAR 框架分为三个阶段，旨在优化上下文增强模型 $\pi_C$：

*   **阶段 0：基于对比学习的智能体反思（Agentic Reflection）**
    *   对训练集中的每个任务进行 $m$ 次独立执行，收集包含成功与失败的轨迹（Trajectories）。
    *   引入一个配备 Shell 工具的反思智能体 $\pi_R$，通过**对比分析**同一任务的不同轨迹，识别导致成功的关键决策和导致失败的陷阱。
    *   生成任务特定的高质量指令 $c_i$，构建监督微调数据集 $D_{SFT} = \{(q_i, c_i)\}$。
*   **阶段 1：监督微调（SFT）**
    *   使用 $D_{SFT}$ 对开源 LLM（如 Qwen3-32B）进行全参数微调，使其初步具备根据任务描述生成增强上下文的能力。
*   **阶段 2：强化学习（RL）优化**
    *   **目标函数**：$\max_{\theta} E_{q, c, \tau} [R(\tau)]$，即最大化执行智能体在增强上下文下的预期奖励。
    *   **关键技术**：采用 **GRPO（Group Relative Policy Optimization）** 算法。在训练过程中，冻结执行智能体 $\pi_E$，仅更新 CAM 的参数。奖励信号直接来源于执行智能体完成任务的实际得分。
*   **推理流程**：新任务 $q_{new} \rightarrow$ CAM 生成 $c_{new} \rightarrow$ 执行智能体接收 $q_{new} \oplus c_{new}$ 进行决策。

#### 3. 实验设计
*   **数据集/场景**：
    *   **AppWorld**：一个模拟真实世界应用交互的复杂基准测试，评估指标为任务目标完成率（TGC）和场景目标完成率（SGC）。
    *   **WebShop-40k**：在线购物任务的简化版（包含 4 万个商品索引），评估指标为平均奖励。
*   **对比方法（Baselines）**：
    *   **Vanilla Baseline**：直接使用执行智能体，无增强。
    *   **RAG**：基于向量相似度检索训练集中的最优指令。
    *   **ACE (Agentic Context Engineering)**：一种基于提示词演化的上下文工程方法。
*   **执行智能体**：使用了 Claude-3.5-Sonnet 和 DeepSeek-V3.1。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA B200 GPU**。
*   **模型规格**：CAM 初始化自 **Qwen3-32B**。
*   **训练细节**：
    *   SFT 阶段：全参数微调，5 个 epoch，学习率 $1 \times 10^{-5}$。
    *   RL 阶段：使用 Verl 框架实现 GRPO，训练 15 个 epoch。
    *   **并行加速**：利用 Amazon Bedrock AgentCore Runtime 并行启动多个容器，以支持高并发的轨迹采集和奖励计算。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准测试上进行了完整评估，每个指标均报告了 3 次运行的平均值及标准差。
*   **消融实验**：非常充分。分别验证了“对比学习（CL）”、“监督微调（SFT）”和“强化学习（RL）”三个组件的必要性，证明了每一阶段都能带来显著的性能增益。
*   **迁移性实验**：测试了在 Claude 轨迹上训练的 CAM 是否能辅助 DeepSeek 执行器，验证了框架的通用性。
*   **客观性**：使用了官方评估套件，且训练集与测试集严格隔离，实验设计较为公平。

#### 6. 主要结论与发现
*   **性能提升**：在 AppWorld 上，CLEAR 将 TGC 从 72.62% 提升至 **81.15%**；在 WebShop 上，平均奖励从 0.68 提升至 **0.74**。
*   **优于检索**：CLEAR 显著优于 RAG 和 ACE，证明了“生成式适配”比“简单检索”更能有效降低执行智能体的推理难度。
*   **跨模型迁移**：CAM 具有良好的迁移性，即使执行智能体发生更换（如从闭源模型换为开源模型），增强效果依然显著。
*   **低延迟开销**：虽然增加了 CAM 的推理步骤，但平均仅增加约 1.2 秒延迟，相对于任务成功率的巨大提升，该开销在可接受范围内。

#### 7. 优点（亮点）
*   **解耦设计**：CAM 与执行智能体解耦，这意味着该框架可以用来增强无法获取权重的闭源模型（如 GPT-4, Claude）。
*   **对比反思机制**：通过对比同一任务的成败轨迹，反思智能体能提取出比单次反思更深刻、更具鲁棒性的策略。
*   **端到端优化**：通过 RL 直接以任务最终奖励为目标优化 CAM，确保生成的上下文确实对“提分”有帮助。

#### 8. 不足与局限
*   **训练成本高**：RL 阶段需要执行大量的任务 Rollouts 来获取奖励信号，这在 API 调用成本或计算资源上开销较大。
*   **环境依赖**：WebShop 实验使用了简化版（40k 商品），可能无法完全代表原始大规模环境下的挑战。
*   **长轨迹处理**：对于极长的执行轨迹，反思智能体仍需依赖正则过滤等启发式方法来避免上下文溢出，可能丢失部分细节。

（完）
