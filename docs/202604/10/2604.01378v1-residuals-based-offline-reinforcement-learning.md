---
title: Residuals-based Offline Reinforcement Learning
title_zh: 基于残差的离线强化学习
authors: "Qing Zhu, Xian Yu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01378v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 离线强化学习中基于残差的贝尔曼最优算子
tldr: 本文针对离线强化学习中数据覆盖受限和分布偏移的挑战，提出了一种基于残差的离线强化学习框架。该框架通过定义一种新型贝尔曼最优算子，利用经验残差将环境动力学模型的估计误差显式引入策略优化过程。研究不仅在理论上证明了该算子的收缩性、渐近最优性及有限样本保证，还开发了相应的离线DQN算法，并在随机CartPole环境中验证了其有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01378v1/fig-001.webp\", \"caption\": \"Fig. 1: Flowchart of the residuals-based offline RL.\", \"page\": 3, \"index\": 1, \"width\": 512, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.01378v1/fig-002.webp\", \"caption\": \"Fig. 2: Comparison of different sample sizes\", \"page\": 7, \"index\": 2, \"width\": 429, \"height\": 577}, {\"url\": \"assets/figures/arxiv/2604.01378v1/fig-003.webp\", \"caption\": \"Fig. 3: Comparison of Models\", \"page\": 8, \"index\": 3, \"width\": 383, \"height\": 516}]"
motivation: 旨在解决离线强化学习在数据覆盖不足时，因环境动力学估计误差和分布偏移导致的策略性能下降问题。
method: 提出一种基于残差的贝尔曼最优算子，通过经验残差显式捕捉并补偿动力学模型学习中的估计误差。
result: 理论证明了该算子具有收缩映射特性和渐近最优性，实验表明基于残差的离线DQN在随机环境中表现优异。
conclusion: 该框架为处理离线强化学习中的模型误差提供了新方案，通过结合残差机制显著提升了策略学习的稳健性。
---

## 摘要
离线强化学习（RL）因其能够从先前收集的数据中学习策略而无需与真实环境交互而受到越来越多的关注，这在涉及高风险的应用中尤为重要。尽管已有大量研究开发了离线强化学习算法，但这些方法通常依赖于对数据覆盖范围的限制性假设，并受到分布偏移的影响。在本文中，我们针对通用状态和动作空间提出了一种基于残差的离线强化学习框架。具体而言，我们定义了一个基于残差的贝尔曼最优算子，该算子通过利用经验残差，将学习转移动态过程中的估计误差显式地纳入策略优化中。我们证明了该贝尔曼算子是一个压缩映射，并确定了其不动点在何种条件下具有渐近最优性并具备有限样本保证。我们进一步开发了一种基于残差的离线深度Q网络（DQN）算法。通过使用随机倒立摆（CartPole）环境，我们展示了基于残差的离线DQN算法的有效性。

## Abstract
Offline reinforcement learning (RL) has received increasing attention for learning policies from previously collected data without interaction with the real environment, which is particularly important in high-stakes applications. While a growing body of work has developed offline RL algorithms, these methods often rely on restrictive assumptions about data coverage and suffer from distribution shift. In this paper, we propose a residuals-based offline RL framework for general state and action spaces. Specifically, we define a residuals-based Bellman optimality operator that explicitly incorporates estimation error in learning transition dynamics into policy optimization by leveraging empirical residuals. We show that this Bellman operator is a contraction mapping and identify conditions under which its fixed point is asymptotically optimal and possesses finite-sample guarantees. We further develop a residuals-based offline deep Q-learning (DQN) algorithm. Using a stochastic CartPole environment, we demonstrate the effectiveness of our residuals-based offline DQN algorithm.

---

## 论文详细总结（自动生成）

这篇论文提出了一种基于残差的离线强化学习（Offline RL）框架，旨在解决离线数据覆盖不足和模型估计误差导致的性能下降问题。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
离线强化学习的核心挑战在于：
*   **数据覆盖限制**：传统方法假设离线数据集必须覆盖目标策略可能访问的所有状态-动作空间，这在连续空间中极难实现。
*   **分布偏移（Distribution Shift）**：训练出的策略所诱导的分布与行为策略分布不一致，导致价值评估不可靠。
*   **反事实查询难题**：在无法与环境交互的情况下，难以准确预测“如果采取了不同动作会发生什么”。
*   **研究动机**：作者借鉴了随机优化中的“经验残差样本平均近似（ER-SAA）”思想，试图通过显式建模转移概率的估计误差（残差），来增强离线策略的稳健性和泛化能力。

### 2. 方法论：核心思想与技术细节
该框架将离线 RL 分为两个阶段：
*   **回归阶段（Regression）**：利用离线数据集 $D_N$ 训练一个监督学习模型 $\hat{f}_N(s, a)$ 来预测下一状态。随后计算**经验残差** $\hat{\epsilon}^{(i)} = s'^{(i)} - \hat{f}_N(s^{(i)}, a^{(i)})$，这些残差捕捉了模型预测与真实动力学之间的差异。
*   **策略优化阶段（Residuals-based RL）**：
    *   **残差贝尔曼算子**：定义了一个新型算子 $\hat{T}_N$，在计算期望价值时，不是只依赖点预测，而是通过从残差集中随机采样并叠加到预测值上：$s' = \Pi_S(\hat{f}_N(s, a) + \hat{\epsilon})$。
    *   **理论保证**：论文证明了该算子是**收缩映射（Contraction Mapping）**，确保了唯一不动点的存在。同时，在特定假设下证明了该不动点具有**渐近最优性**和**有限样本误差界限**。
*   **算法实现**：开发了 **Residuals-based Offline DQN**。在训练过程中，算法在由“回归模型+随机残差采样”构建的模拟器中进行“在线”训练，从而缓解了分布偏移。

### 3. 实验设计
*   **实验场景**：随机倒立摆（Stochastic CartPole）。为了模拟真实世界的不确定性，在状态转移中加入了高斯噪声。
*   **Benchmark 与对比方法**：
    *   **不同样本量对比**：测试了从 500 到 1000 条轨迹的不同数据集规模。
    *   **消融实验（核心对比）**：对比了“有残差（w/ residuals）”模型与“无残差（w/o residuals，仅靠纯回归预测）”的基准模型。
*   **评估指标**：训练奖励（在模拟器中）与测试奖励（在真实随机环境中）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练总时长。
*   **网络结构**：回归模型使用单隐藏层（64个神经元）的神经网络；Q 网络使用双隐藏层（每层 64 个神经元）的全连接网络。
*   **超参数**：学习率 $10^{-4}$，折扣因子 $\gamma=0.99$，使用 Adam 优化器。

### 5. 实验数量与充分性
*   **实验规模**：针对每个设置进行了 **5 次独立运行**并报告了平均结果。
*   **充分性评价**：
    *   **理论验证充分**：实验验证了样本量增加对性能的提升，符合理论预期。
    *   **对比客观**：通过“有无残差”的对比，清晰地展示了残差机制在处理随机动力学时的优越性。
    *   **局限性**：实验仅限于 CartPole 这一基础环境，缺乏在更复杂的离线 RL 标准测试集（如 D4RL）上的验证，实验场景的多样性略显不足。

### 6. 主要结论与发现
*   **残差的重要性**：不带残差的纯回归模型虽然在训练集上表现良好，但在真实随机环境中表现极差，说明其无法处理模型偏差。
*   **性能提升**：基于残差的离线 DQN 在测试环境中的表现显著优于基准模型，且随着离线数据量的增加，策略性能稳步提升。
*   **理论一致性**：实验结果支持了论文关于残差贝尔曼算子能够收敛至渐近最优解的理论推导。

### 7. 优点（亮点）
*   **理论框架完备**：不仅提出了算法，还提供了严谨的数学证明（收缩性、一致性、有限样本保证），这在强化学习论文中具有较高的学术价值。
*   **解决分布偏移的新视角**：通过残差采样模拟真实动力学，允许在“模拟环境”中进行准在线训练，从根本上缓解了离线 RL 的分布偏移问题。
*   **无需强假设**：相比传统方法，该框架不要求数据集对状态-动作空间有完美的均匀覆盖。

### 8. 不足与局限
*   **实验环境过于简单**：CartPole 属于低维控制问题，该方法在处理高维图像输入或复杂多关节机器人任务时的有效性尚待验证。
*   **残差分布假设**：该方法依赖于经验残差能够代表真实噪声分布的假设。如果离线数据存在严重的系统性偏差，残差可能无法纠正模型错误。
*   **计算开销**：在训练过程中进行残差采样和投影操作（$\Pi_S$）可能会增加计算负担，尤其是在状态空间极其复杂的情况下。

（完）
