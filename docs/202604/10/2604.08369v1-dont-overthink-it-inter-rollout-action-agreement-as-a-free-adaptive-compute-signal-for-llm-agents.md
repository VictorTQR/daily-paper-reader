---
title: "Don't Overthink It: Inter-Rollout Action Agreement as a Free Adaptive-Compute Signal for LLM Agents"
title_zh: 别想太多：轨迹间动作一致性作为大语言模型智能体的免费自适应计算信号
authors: Khushal Sethi
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08369v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 大语言模型智能体的自适应计算控制
tldr: "针对LLM智能体在推理时计算资源分配不均的问题，本文提出TrACE。这是一种无需训练的自适应计算控制器，通过衡量多次采样动作的一致性来动态分配计算量：在一致性高的简单步骤快速决策，在一致性低的困难步骤增加采样。实验表明，TrACE在保持与固定预算采样相同精度的同时，显著减少了33%-65%的LLM调用次数，是首个用于多步决策任务的无需训练的自适应计算方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08369v1/fig-001.webp\", \"caption\": \"Figure 1: Compute–accuracy frontier. Each point is one (condition, benchmark) pair. TrACE-4 and TrACE-8 (red, pink) match the accuracy of SC-4 and SC-8 (blue) at substantially fewer LLM calls, Pareto-dominating the self-consistency baseline in both settings. Error bars on MiniHouse are 95% bootstrap CIs; GSM8K bars are Wald CIs from binomial data (no per-task data available for bootstrap, see §6).\", \"page\": 4, \"index\": 1, \"width\": 747, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.08369v1/fig-002.webp\", \"caption\": \"Figure 2: Left: Agreement is a reliable success signal. Right: TrACE call counts are task-adaptive.\", \"page\": 7, \"index\": 2, \"width\": 832, \"height\": 376}, {\"url\": \"assets/figures/arxiv/2604.08369v1/fig-003.webp\", \"caption\": \"Figure 3: τhigh ablation (TrACE-4, kmax = 4). Three thresholds are evaluated: 0.50 (commit at bare majority), 0.75 (default), and 1.00 (require unanimity, degenerates to SC-4). The dotted line shows SC-4 accuracy and call count for reference. TrACE with τ = 0.75 achieves matched accuracy at substantially fewer calls than either extreme. τ = 0.50 underspends and slightly loses accuracy; τ = 1.00 recovers SC-4 accuracy and cost.\", \"page\": 8, \"index\": 3, \"width\": 812, \"height\": 260}]"
motivation: 现有的推理计算缩放方法对所有决策步骤统一分配预算，导致在简单任务上浪费资源而在困难任务上投入不足。
method: 提出TrACE方法，通过测量多个候选动作之间的一致性作为难度信号，动态决定是否需要增加采样量或直接提交决策。
result: "在GSM8K和MiniHouse基准测试中，TrACE在达到与固定采样方法相同准确率的前提下，减少了33%至65%的计算开销。"
conclusion: 模型自身的输出一致性是衡量步骤难度的可靠信号，无需额外训练或验证器即可实现高效的推理资源自适应分配。
---

## 摘要
推理时计算缩放已成为提高大语言模型（LLM）智能体可靠性的强大技术，但现有方法通常统一分配计算资源：无论难度如何，每个决策步骤都获得相同的预算。我们引入了 TrACE（通过一致性实现的轨迹自适应计算），这是一种无需训练的控制器，通过测量轨迹间动作一致性，在智能体的时间步之间自适应地分配 LLM 调用。在每一步中，TrACE 采样一小组候选下一步动作，并测量模型选择相同动作的一致性程度。高一致性预示着决策简单，控制器会立即提交；低一致性则预示着不确定性，控制器会采样额外的轨迹（直至达到可配置的上限），然后提交至多数动作。该方法无需学习组件、外部验证器或人类标签。我们在涵盖单步推理（GSM8K，n=50）和多步家庭导航（MiniHouse，n=30）的两个基准测试上，使用在 CPU 上运行的 Qwen 2.5 3B Instruct 模型，将 TrACE 与贪婪解码及固定预算的自一致性（SC-4, SC-8）进行了对比评估。结果显示，TrACE-4 在 GSM8K 和 MiniHouse 上分别减少了 33% 和 39% 的 LLM 调用，且准确率与 SC-4 持平；TrACE-8 在这两个任务上分别减少了 55% 和 65% 的调用，且准确率与 SC-8 持平。我们进一步证明，轨迹间一致性是步骤级成功的可靠信号，验证了核心假设：模型自身的输出一致性编码了可以在无需训练的情况下利用的难度信息。TrACE 是首个在多步顺序决策任务上进行评估的、针对 LLM 智能体的无需训练、逐时间步自适应计算控制器。

## Abstract
Inference-time compute scaling has emerged as a powerful technique for improving the reliability of large language model (LLM) agents, but existing methods apply compute uniformly: every decision step receives the same budget regardless of its difficulty. We introduce TrACE (Trajectorical Adaptive Compute via agrEement), a training-free controller that allocates LLM calls adaptively across agent timesteps by measuring inter-rollout action agreement. At each step, TrACE samples a small set of candidate next actions and measures how consistently the model commits to the same action. High agreement signals an easy decision; the controller commits immediately. Low agreement signals uncertainty; the controller samples additional rollouts up to a configurable cap before committing to the plurality action. No learned components, no external verifier, and no human labels are required. We evaluate TrACE against greedy decoding and fixed-budget self-consistency (SC-4, SC-8) on two benchmarks spanning single-step reasoning (GSM8K, n=50) and multi-step household navigation (MiniHouse, n=30), using a Qwen 2.5 3B Instruct model running on CPU. TrACE-4 matches SC-4 accuracy while using 33% fewer LLM calls on GSM8K and 39% fewer on MiniHouse. TrACE-8 matches SC-8 accuracy with 55% fewer calls on GSM8K and 65% fewer on MiniHouse. We further show that inter-rollout agreement is a reliable signal of step-level success, validating the core hypothesis that the model's own output consistency encodes difficulty information that can be exploited without training. TrACE is the first training-free, per-timestep adaptive-compute controller for LLM agents to be evaluated on multi-step sequential decision tasks.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **TrACE** 的方法，旨在解决大语言模型（LLM）智能体在推理时计算资源分配效率低下的问题。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在顺序决策任务（如导航、代码编写）中，LLM 智能体面临的每一步决策难度差异巨大。有些步骤显而易见，而有些则充满歧义。然而，现有的推理缩放技术（如自一致性采样）通常对所有步骤分配统一的计算预算，导致在简单步骤上浪费资源，在困难步骤上资源不足。
论文的核心动机是：**能否利用模型自身产生的信息，在无需额外训练、无需外部验证器的情况下，实现计算资源的自适应分配？**

### 2. 方法论：TrACE 核心思想与算法流程
TrACE（Trajectorical Adaptive Compute via agrEement）的核心思想是利用**轨迹间动作一致性（Inter-rollout Action Agreement）**作为难度的代理信号。

*   **核心假设**：如果模型在多次随机采样中一致地选择同一个动作，说明该决策简单且可靠；如果采样结果分散，说明模型不确定，需要更多计算量来寻找最优解。
*   **算法流程**：
    1.  **初始采样**：在当前时间步 $t$，以温度 $\tau$ 独立采样 $k_{init}$（默认=2）个候选动作。
    2.  **计算一致性**：计算多数动作出现的频率 $\alpha_t$。
    3.  **自适应决策**：
        *   如果 $\alpha_t \ge \tau_{high}$（预设阈值，默认=0.75），则立即提交该动作，结束当前步。
        *   如果 $\alpha_t < \tau_{high}$，则逐个增加采样，直到一致性达标或达到最大采样上限 $k_{max}$（如 4 或 8）。
    4.  **规范化**：使用动作规范化器（Canonicaliser）处理字符串（如去空格、转小写），确保一致性计算的准确性。

### 3. 实验设计
*   **数据集/场景**：
    *   **GSM8K**：单步数学推理基准，测试该信号在非顺序任务中的泛化性（n=50）。
    *   **MiniHouse**：论文作者开发的轻量级多步家庭导航环境，包含房间导航、物体移动等任务（n=30）。
*   **对比方法（Baselines）**：
    *   **Greedy**：贪婪解码（k=1）。
    *   **Self-Consistency (SC-4, SC-8)**：固定预算的自一致性采样，每步固定调用 4 次或 8 次 LLM。
    *   **TrACE-4, TrACE-8**：本文提出的自适应方法，上限分别为 4 和 8。

### 4. 资源与算力
*   **硬件**：实验在 **Apple M 系列 CPU** 上运行（32GB RAM），使用 8 个推理线程。
*   **模型**：使用 **Qwen 2.5 3B Instruct** 模型，并进行了 Q4_K_M 量化（GGUF 格式）。
*   **算力特点**：论文强调了“CPU 友好型”评估，未提及 GPU 使用。由于是推理端优化且无需训练，不涉及训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验涵盖了两个不同性质的基准测试。
    *   MiniHouse 任务进行了 3 个随机种子的重复实验（共 90 个任务-种子对），并提供了 95% 置信区间。
    *   进行了关于阈值 $\tau_{high}$ 的**消融实验**。
*   **充分性评价**：实验设计较为客观，直接对比了相同精度下的计算开销。虽然样本量（n=30, n=50）不算巨大，但对于验证“推理效率提升”这一核心主张是足够的。不过，缺乏在更大规模模型（如 70B+）上的验证。

### 6. 主要结论与发现
*   **帕累托改进**：TrACE 在保持与 SC-4/SC-8 相同准确率的前提下，显著减少了 LLM 调用次数。在 GSM8K 上减少了 **33%-55%**，在 MiniHouse 上减少了 **39%-65%**。
*   **信号有效性**：实验证明，动作一致性与步骤的最终成功率呈正相关，验证了其作为难度信号的可靠性。
*   **时间效率**：在实际运行中，TrACE-8 比 SC-8 节省了约 **65% 的墙钟时间**。

### 7. 优点与亮点
*   **完全无需训练（Training-free）**：不需要昂贵的进程奖励模型（PRM）或外部验证器，即插即用。
*   **计算高效**：通过简单的统计一致性实现自适应，几乎没有额外的计算开销。
*   **开源贡献**：提供了 MiniHouse 这一无需复杂依赖（如 C++ 编译）的轻量级智能体评估环境。

### 8. 不足与局限
*   **模型规模限制**：仅在 3B 模型上测试，大模型的校准特性可能不同，结论是否能完美迁移至 70B 或 GPT-4 级别模型尚待验证。
*   **任务复杂度**：MiniHouse 相对简单，且 3B 模型在多步规划上存在性能瓶颈（准确率在 0.37 左右触顶），导致效率提升虽大，但绝对准确率提升有限。
*   **开放式生成挑战**：对于代码编写等输出空间极大的任务，简单的字符串规范化可能难以准确衡量“一致性”。
*   **超参数敏感性**：阈值 $\tau_{high}$ 需要手动设置，缺乏一种根据计算预算自动调整阈值的理论方法。

（完）
