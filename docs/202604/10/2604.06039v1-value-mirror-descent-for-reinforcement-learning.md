---
title: Value Mirror Descent for Reinforcement Learning
title_zh: 强化学习中的价值镜像下降
authors: "Zhichao Jia, Guanghui Lan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06039v1"
tags: ["query:rl-agt"]
score: 10.0
evidence: 结合镜像下降的新型价值优化强化学习算法
tldr: 本文提出了一种名为价值镜像下降（VMD）的新型强化学习算法，将凸优化的镜像下降思想融入经典的价值迭代框架。针对生成模型下的随机环境，研究者进一步开发了结合方差缩减技术的SVMD算法。该方法在处理一般凸正则化项时达到了近乎最优的样本复杂度，并首次保证了生成策略与最优策略间的Bregman散度有界，为离线训练后的在线持续学习奠定了理论基础。
source: arxiv
selection_source: fresh_fetch
motivation: 旨在解决现有价值迭代方法在样本复杂度及离线转在线学习中策略稳定性（Bregman散度有界性）方面的不足。
method: 将镜像下降法集成到价值迭代框架中，并针对随机环境引入方差缩减技术构建了SVMD算法。
result: SVMD在一般凸正则化下实现了近优的样本复杂度，并证明了生成策略与最优策略间的Bregman散度在迭代过程中保持有界。
conclusion: 该研究通过融合优化理论与强化学习，为实现高效且具备策略稳定性的强化学习算法提供了新的理论贡献和分析工具。
---

## 摘要
价值迭代类方法在强化学习（RL）中为计算近优价值函数而得到了广泛研究。在生成采样模型下，这些方法可以实现比策略优化方法更优的样本复杂度，特别是在对折扣因子的依赖方面。在实践中，它们通常被用于离线训练或模拟环境。在本文中，我们考虑具有状态空间 S、动作空间 A、折扣因子 $γ\in(0,1)$ 且代价在 $[0,1]$ 范围内的折扣马尔可夫决策过程。我们提出了一种新颖的价值优化方法，称为价值镜像下降（VMD），它将凸优化中的镜像下降集成到经典的价值迭代框架中。在转移核已知的确定性设置下，我们证明了 VMD 具有线性收敛性。针对具有生成模型的随机设置，我们开发了一种随机变体 SVMD，它结合了随机价值迭代类方法中常用的方差缩减技术。对于具有一般凸正则项的 RL 问题，SVMD 达到了 $\tilde{O}(|S||A|(1-γ)^{-3}ε^{-2})$ 的近优样本复杂度。此外，我们证明了在整个迭代过程中，生成的策略与最优策略之间的 Bregman 散度保持有界。这一性质在现有的随机价值迭代类方法中是不具备的，但对于在离线训练后实现有效的在线（持续）学习至关重要。在强凸正则项下，SVMD 实现了 $\tilde{O}(|S||A|(1-γ)^{-5}ε^{-1})$ 的样本复杂度，提升了在高精度区域的性能。此外，我们证明了生成的策略收敛至最优策略。总之，所提出的方法、其分析以及由此产生的保证，为 RL 和优化领域的文献做出了新贡献。

## Abstract
Value iteration-type methods have been extensively studied for computing a nearly optimal value function in reinforcement learning (RL). Under a generative sampling model, these methods can achieve sharper sample complexity than policy optimization approaches, particularly in their dependence on the discount factor. In practice, they are often employed for offline training or in simulated environments. In this paper, we consider discounted Markov decision processes with state space S, action space A, discount factor $γ\in(0,1)$ and costs in $[0,1]$. We introduce a novel value optimization method, termed value mirror descent (VMD), which integrates mirror descent from convex optimization into the classical value iteration framework. In the deterministic setting with known transition kernels, we show that VMD converges linearly. For the stochastic setting with a generative model, we develop a stochastic variant, SVMD, which incorporates variance reduction commonly used in stochastic value iteration-type methods. For RL problems with general convex regularizers, SVMD attains a near-optimal sample complexity of $\tilde{O}(|S||A|(1-γ)^{-3}ε^{-2})$. Moreover, we establish that the Bregman divergence between the generated and optimal policies remains bounded throughout the iterations. This property is absent in existing stochastic value iteration-type methods but is important for enabling effective online (continual) learning following offline training. Under a strongly convex regularizer, SVMD achieves sample complexity of $\tilde{O}(|S||A|(1-γ)^{-5}ε^{-1})$, improving performance in the high-accuracy regime. Furthermore, we prove convergence of the generated policy to the optimal policy. Overall, the proposed method, its analysis, and the resulting guarantees, constitute new contributions to the RL and optimization literature.

---

## 论文详细总结（自动生成）

这是一份关于论文《Value Mirror Descent for Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在强化学习（RL）中，存在两种主流算法范式：**价值迭代（VI）类方法**和**策略优化（PO）类方法**。VI类方法在生成模型下具有更优的样本复杂度（对折扣因子 $(1-\gamma)^{-1}$ 的依赖更小），但策略稳定性差且难以处理强凸正则化；PO类方法虽然策略稳定、易于处理正则化，但样本复杂度通常较高。
*   **核心问题**：如何设计一种算法，既能保持价值迭代类方法的高样本效率，又能具备策略优化方法的稳定性和处理正则化项的能力？
*   **研究动机**：现有的价值迭代方法在处理强凸正则化 RL 问题时，无法达到 $O(\epsilon^{-1})$ 的样本复杂度，且生成的策略在迭代过程中可能与最优策略偏离过大（Bregman 散度无界），不利于离线训练后的在线持续学习。

### 2. 论文提出的方法论
论文提出了一种名为**价值镜像下降（Value Mirror Descent, VMD）**的新框架，其核心思想是将凸优化中的镜像下降（Mirror Descent）融入价值迭代。
*   **核心思想**：不再是简单地进行贪婪的价值更新，而是通过一个“近端映射（Prox-mapping）”步骤来更新策略，从而在改善策略和保持策略稳定性之间取得平衡。
*   **关键技术细节**：
    *   **VMD 算法（确定性）**：交替进行策略更新和价值更新。策略更新公式为：$\pi_{t+1} = \arg\min_{\pi} \{ \eta [ \langle c + \gamma P V_t, \pi \rangle + h(\pi) ] + D(\pi, \pi_t) \}$，其中 $D$ 是 Bregman 散度，$h$ 是正则项。
    *   **SVMD 算法（随机版本）**：针对转移核未知的环境，引入了**方差缩减（Variance Reduction）**技术。它通过在每个 Epoch 开始时计算一个高精度的参考价值估计，并在后续迭代中仅采样增量变化，从而降低估计误差。
    *   **单调性维护**：在随机更新中，算法通过分量比较（取最小值操作）来强制维持价值函数的近似单调下降特性。

### 3. 实验设计
*   **场景与数据集**：本论文属于**纯理论分析论文**，未涉及真实世界数据集或 Atari/MuJoCo 等仿真环境的数值实验。
*   **Benchmark（基准）**：论文的对比基准是强化学习理论界公认的**样本复杂度下界（Lower Bound）**以及现有的 SOTA 理论结果，如：
    *   经典的价值迭代（Value Iteration）。
    *   方差缩减 Q 价值迭代（Variance-reduced Q-value iteration）。
    *   随机策略镜像下降（Stochastic Policy Mirror Descent）。
*   **对比维度**：主要对比在不同正则化条件下（一般凸 vs 强凸）达到 $\epsilon$-最优策略所需的样本总数，以及策略的稳定性（Bregman 散度界限）。

### 4. 资源与算力
*   **算力说明**：由于本文是纯数学理论证明，**文中未提及任何关于 GPU、CPU 型号、数量或具体训练时长的信息**。
*   **资源消耗**：研究资源主要体现为数学推导和算法复杂度的理论分析。

### 5. 实验数量与充分性
*   **实验充分性评估**：虽然没有数值实验，但论文在理论层面做了极其充分的论证。
*   **覆盖范围**：论文分别针对“已知环境的确定性 VMD”、“一般凸正则化的随机 SVMD”以及“强凸正则化的随机 SVMD”三种场景给出了严密的收敛性证明。
*   **客观性**：其结论基于严格的数学定理（Theorem 1-3），在理论框架内是客观且公允的。

### 6. 论文的主要结论与发现
*   **线性收敛性**：在确定性环境下，VMD 能够以线性速率收敛，迭代复杂度为 $\tilde{O}((1-\gamma)^{-1})$。
*   **近优样本复杂度**：在一般凸正则化下，SVMD 达到了 $\tilde{O}(|S||A|(1-\gamma)^{-3}\epsilon^{-2})$ 的样本复杂度，这在 $(1-\gamma)$ 和 $\epsilon$ 的依赖上达到了理论下界。
*   **强凸性能突破**：在强凸正则化下，SVMD 实现了 $\tilde{O}(\epsilon^{-1})$ 的样本复杂度。**这是首个达到此复杂度的价值类方法**。
*   **策略稳定性保证**：证明了生成策略与最优策略之间的 Bregman 散度在迭代中保持有界。这意味着该算法产出的策略非常适合作为在线微调的起点。

### 7. 优点
*   **理论创新**：成功桥接了优化理论（镜像下降）与动态规划（价值迭代），为 RL 算法设计提供了新视角。
*   **高效性与稳定性并存**：既保留了价值迭代对折扣因子的高效利用，又获得了策略优化方法的稳定性。
*   **持续学习潜力**：Bregman 散度有界的结论为解决离线预训练到在线部署的平滑过渡提供了理论依据。

### 8. 不足与局限
*   **缺乏数值验证**：论文完全基于理论推导，没有在复杂的深度强化学习任务中进行实证，实际表现（如在神经网络近似下的表现）尚不可知。
*   **表格化限制**：目前的理论分析主要针对有限状态和动作空间（Tabular MDP），对于高维连续空间的泛化能力有待进一步研究。
*   **超参数依赖**：算法实现依赖于精确设计的步长 $\eta$ 和采样数 $m$，这些参数在实际应用中可能难以微调。

（完）
