---
title: Asymmetric Actor-Critic for Multi-turn LLM Agents
title_zh: 面向多轮 LLM 智能体的非对称 Actor-Critic 框架
authors: "Shuli Jiang, Zhaoyang Zhang, Yi Zhang, Shuo Yang, Wei Xia, Stefano Soatto"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00304v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 针对大语言模型智能体的非对称演员-评论家框架
tldr: 针对多轮对话智能体在单次尝试中可靠性不足的问题，本文提出一种非对称 Actor-Critic 框架。该框架利用强大的闭源模型作为 Actor 负责生成，配合轻量级开源模型作为 Critic 进行实时监控与干预。通过利用生成与验证之间的不对称性，该方法在无需修改 Actor 的情况下，通过微调 Critic 显著提升了智能体在复杂任务中的成功率和可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00304v1/fig-001.webp\", \"caption\": \"Figure 1: Existing actor–critic methods assume trainable actors and structured action spaces. We propose an asymmetric actor–critic framework where a lightweight open-source critic supervises a fixed proprietary actor at runtime, enabling reliable control for one-shot multiturn conversational agents.\", \"page\": 1, \"index\": 1, \"width\": 458, \"height\": 291}, {\"url\": \"assets/figures/arxiv/2604.00304v1/fig-002.webp\", \"caption\": \"Table 13: Statistics of the generated critic training data. Each critic evaluation point in a trajectory corresponds to one training sample. Positive samples denote critic rejections, while negative samples denote approvals. For UserBench, the critic is trained on data aggregated across all environments.\", \"page\": 18, \"index\": 2, \"width\": 779, \"height\": 204}]"
motivation: 现有的智能体优化方法通常依赖多次尝试或模型微调，难以解决闭源大模型在不可重试的实时多轮交互中的可靠性问题。
method: 采用非对称架构，由高性能闭源模型担任 Actor，轻量级开源模型担任 Critic 进行实时监督干预，并引入了专门的 Critic 微调数据生成流水线。
result: 在 τ-bench 和 UserBench 实验中，该框架显著优于单智能体基准，且微调后的轻量级 Critic 表现可媲美甚至超越大型闭源模型。
conclusion: 研究证明了通过小型模型对大型闭源模型进行实时监督的有效性，为构建可靠的多轮对话智能体提供了一种高效且实用的方案。
---

## 摘要
大语言模型（LLMs）展现出强大的推理和对话能力，但在多轮交互中确保行为的可靠性仍然具有挑战性。在许多实际应用中，智能体必须在无法重试的单次尝试场景下取得成功。现有方法要么依赖于反思或事后评估（这需要额外的尝试），要么假设模型完全可训练，从而无法利用闭源 LLMs。我们提出了一种用于构建可靠对话智能体的非对称 Actor-Critic 框架。在该框架中，一个强大的闭源 LLM 担任 Actor，而一个较小的开源 Critic 提供运行时监督，监控 Actor 的动作并在同一交互轨迹内进行干预。与基于训练的 Actor-Critic 方法不同，我们的框架监督的是在开放式对话环境中运行的固定 Actor。该设计利用了生成与验证的不对称性：虽然高质量的生成需要大模型，但有效的监督通常可以通过较小的模型来实现。我们进一步引入了一个数据生成流水线，在不修改 Actor 的情况下产生用于 Critic 微调的监督信号。在 $τ$-bench 和 UserBench 上的实验表明，我们的方法相比强大的单智能体基线显著提高了可靠性和任务成功率。此外，轻量级开源 Critic 在担任 Critic 角色时可以媲美甚至超越更大的闭源模型，且 Critic 微调相比几种最先进的方法带来了额外的性能提升。

## Abstract
Large language models (LLMs) exhibit strong reasoning and conversational abilities, but ensuring reliable behavior in multi-turn interactions remains challenging. In many real-world applications, agents must succeed in one-shot settings where retries are impossible. Existing approaches either rely on reflection or post-hoc evaluation, which require additional attempts, or assume fully trainable models that cannot leverage proprietary LLMs. We propose an asymmetric actor-critic framework for reliable conversational agents. A powerful proprietary LLM acts as the actor, while a smaller open-source critic provides runtime supervision, monitoring the actor's actions and intervening within the same interaction trajectory. Unlike training-based actor-critic methods, our framework supervises a fixed actor operating in open-ended conversational environments. The design leverages a generation-verification asymmetry: while high-quality generation requires large models, effective oversight can often be achieved by smaller ones. We further introduce a data generation pipeline that produces supervision signals for critic fine-tuning without modifying the actor. Experiments on $τ$-bench and UserBench show that our approach significantly improves reliability and task success over strong single-agent baselines. Moreover, lightweight open-source critics rival or surpass larger proprietary models in the critic role, and critic fine-tuning yields additional gains over several state-of-the-art methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Asymmetric Actor-Critic for Multi-turn LLM Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**多轮对话智能体在现实应用中的可靠性**。
*   **研究动机**：虽然 LLM 智能体能力强大，但在处理复杂业务规则、领域约束和用户偏好时，常会出现幻觉、违反政策或推理错误。
*   **现实挑战**：在许多面向客户的场景中，智能体只有“一次机会”（One-shot），无法通过多次重试或事后反思来纠正错误。
*   **现有局限**：现有的 Actor-Critic 框架通常假设 Actor 是可训练的（不适用于闭源模型），且多针对结构化动作空间（如游戏或简单工具调用），难以处理开放式的多轮对话。

### 2. 方法论：非对称 Actor-Critic 框架
论文提出了一种**非对称 Actor-Critic (Asym-AC)** 架构，核心思想是利用“生成与验证的不对称性”。
*   **核心组件**：
    *   **Actor ($\pi_A$)**：高性能、固定的闭源模型（如 Claude-4 Sonnet），负责生成对话、调用工具和决策。
    *   **Critic ($C_\theta$)**：轻量级、可训练的开源模型（如 Qwen3-8B），负责实时监控 Actor 的提议并决定是否干预。
*   **运行时监督流程**：
    1.  Actor 针对当前历史 $h_t$ 提出一个候选动作 $\hat{a}_t$。
    2.  Critic 评估该动作。如果发现违反约束，则生成反馈信号 $c_t$（如拒绝理由或修正建议）。
    3.  Actor 根据反馈 $c_t$ 重新生成动作 $a_t$。
    4.  最终动作执行，进入下一轮。
*   **Critic 训练流水线**：引入了“自我博弈、Critic 在环”的数据生成方法。通过将现有对话数据集重放（Replay），识别出单 Actor 失败但 Actor-Critic 协作成功的“硬任务”，提取 Critic 的干预信号进行监督微调（SFT）。

### 3. 实验设计
*   **数据集/场景**：
    *   **$\tau$-bench**：模拟零售和航空领域的客户服务，强调政策合规性和多步工具调用。
    *   **UserBench**：模拟旅游规划，要求智能体通过多轮对话推断用户隐式偏好并做出最优推荐。
*   **对比基准（Baselines）**：
    *   **Single Actor**：仅使用闭源模型（Claude-4 Sonnet）。
    *   **CRITIC**：基于提示词的自我修正方法。
    *   **ReAct**：经典的推理-动作交替框架。
    *   **APIGen-MT / IRMA**：针对特定基准的最先进（SOTA）方法。
*   **消融实验**：对比了不同规模的 Critic（4B, 8B, 32B）以及闭源模型作为 Critic 的效果。

### 4. 资源与算力
*   **模型使用**：Actor 统一使用 Claude-4 Sonnet。Critic 使用了 Qwen3 系列（4B, 8B, 32B）。
*   **训练细节**：使用 TRL 框架进行微调，采用 LoRA 技术（Rank=16, $\alpha$=32）。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到微调是在 5-15 个 Epoch 内完成，且使用了梯度累积（16步），属于参数高效微调，算力需求相对较低。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准的多个子领域（零售、航空、T-22/33/44）进行了测试。每个任务均运行 5 次独立实验以计算平均成功率（Pass@1）或得分。
*   **充分性**：实验涵盖了从“无微调”到“有微调”的对比，以及不同模型容量的横向对比，验证了框架的泛化性。
*   **客观性**：通过使用标准化的 Benchmark 和与多种 SOTA 方法对比，实验结果具有较高的说服力。

### 6. 主要结论与发现
*   **非对称架构有效**：即使不进行微调，引入轻量级 Critic 也能显著提升闭源 Actor 的任务成功率。
*   **生成-验证差距**：实验发现，较小的开源模型（如 Qwen3-8B）在 Critic 角色上的表现往往优于比它大得多的闭源模型（如 Claude-4），证明了监督任务并不一定需要极大规模的模型。
*   **微调增益**：通过论文提出的数据流水线微调后的 Critic（Asym-AC-SFT），在所有指标上均达到了最优性能。

### 7. 优点
*   **实用性强**：解决了闭源模型无法微调的痛点，适合企业级应用。
*   **低成本**：利用轻量级模型作为“监工”，在提升性能的同时控制了推理成本。
*   **实时干预**：在单次轨迹内完成修正，避免了重试带来的延迟和用户体验下降。
*   **数据高效**：提出了一种将现有对话数据转化为 Critic 监督信号的巧妙方法。

### 8. 不足与局限
*   **延迟增加**：由于每轮关键决策都需要 Critic 介入，会增加系统的整体响应延迟。
*   **依赖 Actor 的理解力**：如果 Actor 本身推理能力极差，即使 Critic 指出错误，Actor 可能也无法根据反馈做出正确修正。
*   **干预时机依赖人工定义**：实验中 $\delta_t$（是否干预）的触发逻辑（如调用工具时、做出最终推荐时）仍需根据任务场景预先定义，缺乏完全自动化的触发机制。
*   **算力细节缺失**：未提供具体的训练资源消耗数据，不便于复现时的成本评估。

（完）
