---
title: Value Mirror Descent for Reinforcement Learning
title_zh: 强化学习中的价值镜像下降法
authors: "Zhichao Jia, Guanghui Lan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06039v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 强化学习理论中的价值镜像下降算法
tldr: 本文提出了一种名为价值镜像下降（VMD）的新型强化学习算法，将凸优化中的镜像下降机制融入经典的价值迭代框架中。针对折扣马尔可夫决策过程，该方法在确定性环境下具有线性收敛性。在随机生成模型下，结合方差缩减技术的SVMD算法在一般凸正则化下达到了近乎最优的样本复杂度，并保证了生成策略与最优策略间的Bregman散度有界，有利于离线训练后的在线持续学习。
source: arxiv
selection_source: fresh_fetch
motivation: 旨在结合价值迭代的高样本效率与镜像下降的优化优势，解决现有随机价值迭代方法在策略稳定性及在线学习衔接方面的不足。
method: 提出VMD算法及其随机变体SVMD，通过在价值迭代中引入镜像下降和方差缩减技术来协同优化策略和价值函数。
result: SVMD在一般凸正则化下实现了近乎最优的样本复杂度，并证明了策略的收敛性以及生成策略与最优策略间Bregman散度的有界性。
conclusion: 该研究为强化学习提供了一种兼具高样本效率和良好策略特性的新优化框架，对离线与在线学习的结合具有重要理论意义。
---

## 摘要
价值迭代类方法在强化学习（RL）中被广泛研究，用于计算近优价值函数。在生成式采样模型下，这些方法可以实现比策略优化方法更优的样本复杂度，特别是在对折扣因子的依赖性方面。在实践中，它们通常用于离线训练或模拟环境。在本文中，我们考虑具有状态空间 S、动作空间 A、折扣因子 $γ\in(0,1)$ 且代价在 $[0,1]$ 范围内的折扣马尔可夫决策过程。我们提出了一种新型价值优化方法，称为价值镜像下降（VMD），它将凸优化中的镜像下降集成到经典的价值迭代框架中。在转移核已知的确定性设置下，我们证明了 VMD 线性收敛。对于具有生成模型的随机设置，我们开发了一个随机变体 SVMD，它结合了随机价值迭代类方法中常用的方差缩减技术。对于具有一般凸正则化项的 RL 问题，SVMD 达到了 $\tilde{O}(|S||A|(1-γ)^{-3}ε^{-2})$ 的近优样本复杂度。此外，我们证明了生成的策略与最优策略之间的 Bregman 散度在整个迭代过程中保持有界。这一特性在现有的随机价值迭代类方法中是不存在的，但对于在离线训练后实现有效的在线（持续）学习至关重要。在强凸正则化项下，SVMD 实现了 $\tilde{O}(|S||A|(1-γ)^{-5}ε^{-1})$ 的样本复杂度，提高了在高精度区域的性能。此外，我们证明了生成的策略收敛于最优策略。总的来说，所提出的方法、其分析以及由此产生的保证，为 RL 和优化领域做出了新的贡献。

## Abstract
Value iteration-type methods have been extensively studied for computing a nearly optimal value function in reinforcement learning (RL). Under a generative sampling model, these methods can achieve sharper sample complexity than policy optimization approaches, particularly in their dependence on the discount factor. In practice, they are often employed for offline training or in simulated environments. In this paper, we consider discounted Markov decision processes with state space S, action space A, discount factor $γ\in(0,1)$ and costs in $[0,1]$. We introduce a novel value optimization method, termed value mirror descent (VMD), which integrates mirror descent from convex optimization into the classical value iteration framework. In the deterministic setting with known transition kernels, we show that VMD converges linearly. For the stochastic setting with a generative model, we develop a stochastic variant, SVMD, which incorporates variance reduction commonly used in stochastic value iteration-type methods. For RL problems with general convex regularizers, SVMD attains a near-optimal sample complexity of $\tilde{O}(|S||A|(1-γ)^{-3}ε^{-2})$. Moreover, we establish that the Bregman divergence between the generated and optimal policies remains bounded throughout the iterations. This property is absent in existing stochastic value iteration-type methods but is important for enabling effective online (continual) learning following offline training. Under a strongly convex regularizer, SVMD achieves sample complexity of $\tilde{O}(|S||A|(1-γ)^{-5}ε^{-1})$, improving performance in the high-accuracy regime. Furthermore, we prove convergence of the generated policy to the optimal policy. Overall, the proposed method, its analysis, and the resulting guarantees, constitute new contributions to the RL and optimization literature.

---

## 论文详细总结（自动生成）

这是一份关于学术论文《Value Mirror Descent for Reinforcement Learning》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
本文探讨了强化学习（RL）中**折扣马尔可夫决策过程（DMDP）**的最优策略求解问题。
*   **研究动机**：目前 RL 算法主要分为“价值迭代类”和“策略优化类”。价值迭代类方法在生成模型（Generative Model）下具有更优的样本复杂度，但存在两个主要缺陷：
    1.  **正则化处理能力弱**：在处理带有强凸正则化项的 RL 问题时，难以达到 $O(\epsilon^{-1})$ 的高精度样本复杂度。
    2.  **策略不稳定性**：生成的近优策略可能在 Bregman 散度上远离最优策略，导致离线预训练后的模型难以直接用于在线持续学习（Fine-tuning）。
*   **核心问题**：如何设计一种算法，既能保持价值迭代的高样本效率，又能具备策略优化方法的稳定性和对正则化项的良好支持。

### 2. 方法论：核心思想与算法流程
论文提出了一种名为 **价值镜像下降（Value Mirror Descent, VMD）** 的新框架及其随机变体 **SVMD**。
*   **核心思想**：将凸优化中的“镜像下降”机制嵌入到传统的“价值迭代”中。算法不再是简单地进行贪婪价值更新，而是通过近端映射（Prox-mapping）来平滑地更新策略。
*   **关键技术细节**：
    *   **VMD（确定性设置）**：交替进行两个步骤：
        1.  **策略更新**：基于当前价值函数，通过最小化包含代价项、未来价值期望及 Bregman 散度正则项的目标函数来计算新策略。
        2.  **价值更新**：利用新策略更新价值向量。
    *   **SVMD（随机设置）**：在无法直接获取转移核的情况下，利用生成模型采样。
        *   **方差缩减（Variance Reduction）**：引入类似双层循环的结构，在外层计算全量估计，内层进行增量更新，以降低采样噪声。
        *   **单调性维护**：在更新价值函数时，通过逐分量取最小值（Min-operation）的操作，强制维持价值函数的近似单调递减特性。
    *   **强凸分析**：针对强凸正则化，引入了“虚拟价值向量序列”作为代理进行收敛性分析。

### 3. 实验设计
*   **研究性质**：本论文属于**纯理论证明型论文**，旨在提供数学上的收敛性保证和样本复杂度分析。
*   **Benchmark 与对比对象**：
    *   对比对象主要是现有的理论最优界限，如 **Variance-reduced Q-value iteration** (Sidford et al., 2018) 和 **Policy Mirror Descent** (Lan, 2023)。
    *   对比指标为**样本复杂度**（达到 $\epsilon$-最优策略所需的总采样数）以及**策略稳定性**（Bregman 散度界限）。

### 4. 资源与算力
*   **说明**：由于本文是纯理论推导，**文中未提及任何 GPU、CPU 等算力资源的使用情况**，亦无实际的训练时长记录。所有的结论均通过数学定理（Theorem）和引理（Lemma）推导得出。

### 5. 实验数量与充分性
*   **理论验证**：论文通过严密的数学证明（占据了 27 页篇幅的大部分）验证了算法在三种设置下的表现：
    1.  **确定性环境**：证明了线性收敛率。
    2.  **一般凸正则化**：证明了 $\tilde{O}(|S||A|(1-\gamma)^{-3}\epsilon^{-2})$ 的样本复杂度，这在对 $(1-\gamma)$ 的依赖上达到了理论下界。
    3.  **强凸正则化**：证明了 $\tilde{O}(|S||A|(1-\gamma)^{-5}\epsilon^{-1})$ 的样本复杂度。
*   **充分性评价**：从理论完备性角度看，论文覆盖了从确定性到随机性、从一般凸到强凸的多种场景，逻辑自洽，推导详尽。但由于缺乏数值仿真实验，算法在复杂高维环境下的实际表现尚待验证。

### 6. 主要结论与发现
*   **样本复杂度突破**：SVMD 是首个在价值迭代框架下，针对正则化 RL 问题实现 $O(\epsilon^{-1})$ 样本依赖度的算法（此前多为 $O(\epsilon^{-2})$）。
*   **策略稳定性保证**：证明了输出策略与最优策略之间的 Bregman 散度是有界的。这意味着该算法产生的策略不仅价值高，而且“行为”上接近最优，非常适合作为在线学习的预训练模型。
*   **收敛性**：在确定性情况下，VMD 达到了与经典价值迭代一致的线性收敛速度。

### 7. 优点
*   **理论创新**：成功融合了镜像下降（优化领域）与价值迭代（动态规划领域）两大范式。
*   **稳定性强**：解决了传统价值类方法输出策略波动大、难以衔接在线学习的痛点。
*   **精度高**：在强凸正则化下显著提升了高精度要求（小 $\epsilon$）时的样本效率。

### 8. 不足与局限
*   **缺乏实证**：完全没有数值实验，无法直观观察算法在 Atari 或 MuJoCo 等标准 RL 基准环境中的表现。
*   **参数依赖**：在强凸设置下，样本复杂度对折扣因子 $(1-\gamma)$ 的依赖达到了 $-5$ 次幂，相比一般凸设置的 $-3$ 次幂有所退化，这表明在长程决策问题中可能需要极大量的样本。
*   **实现复杂度**：相比简单的 Q-learning，SVMD 涉及方差缩减和复杂的近端映射计算，在实际编程实现上更为复杂。

（完）
