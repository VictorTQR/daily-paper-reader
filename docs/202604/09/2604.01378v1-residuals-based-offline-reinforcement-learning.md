---
title: Residuals-based Offline Reinforcement Learning
title_zh: 基于残差的离线强化学习
authors: "Qing Zhu, Xian Yu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01378v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 基于残差的离线强化学习贝尔曼最优算子
tldr: 针对离线强化学习中数据覆盖受限和分布偏移的挑战，本文提出了一种基于残差的离线强化学习框架。该框架通过定义一个结合经验残差的贝尔曼最优算子，将环境动力学学习中的估计误差显式引入策略优化。理论证明该算子具有收缩性及渐近最优性，并据此开发了基于残差的离线DQN算法，在随机CartPole环境中验证了其有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01378v1/fig-001.webp\", \"caption\": \"Fig. 1: Flowchart of the residuals-based offline RL.\", \"page\": 3, \"index\": 1, \"width\": 512, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.01378v1/fig-002.webp\", \"caption\": \"Fig. 2: Comparison of different sample sizes\", \"page\": 7, \"index\": 2, \"width\": 429, \"height\": 577}, {\"url\": \"assets/figures/arxiv/2604.01378v1/fig-003.webp\", \"caption\": \"Fig. 3: Comparison of Models\", \"page\": 8, \"index\": 3, \"width\": 383, \"height\": 516}]"
motivation: 现有的离线强化学习方法通常依赖于严格的数据覆盖假设，且在面对分布偏移时表现出较差的鲁棒性。
method: 提出一种基于残差的贝尔曼最优算子，利用经验残差将转移概率的估计误差显式整合到策略优化过程中。
result: 理论上证明了该算子是收缩映射并具有渐近最优性，实验表明基于残差的离线DQN在随机环境中表现出色。
conclusion: 该研究为处理离线强化学习中的模型误差提供了一种新的理论框架，并为复杂状态动作空间下的策略优化提供了有效工具。
---

## 摘要
离线强化学习（RL）因其能够从先前收集的数据中学习策略而无需与真实环境交互而受到越来越多的关注，这在具有高风险的应用中尤为重要。尽管已有大量工作开发了离线强化学习算法，但这些方法通常依赖于对数据覆盖范围的限制性假设，并受到分布偏移的影响。在本文中，我们针对一般状态和动作空间提出了一种基于残差的离线强化学习框架。具体而言，我们定义了一个基于残差的贝尔曼最优算子，该算子通过利用经验残差，将学习转移动态过程中的估计误差显式地纳入策略优化中。我们证明了该贝尔曼算子是一个收缩映射，并确定了其不动点在何种条件下具有渐近最优性并具备有限样本保证。我们进一步开发了一种基于残差的离线深度Q网络（DQN）算法。通过使用随机倒立摆（CartPole）环境，我们证明了基于残差的离线DQN算法的有效性。

## Abstract
Offline reinforcement learning (RL) has received increasing attention for learning policies from previously collected data without interaction with the real environment, which is particularly important in high-stakes applications. While a growing body of work has developed offline RL algorithms, these methods often rely on restrictive assumptions about data coverage and suffer from distribution shift. In this paper, we propose a residuals-based offline RL framework for general state and action spaces. Specifically, we define a residuals-based Bellman optimality operator that explicitly incorporates estimation error in learning transition dynamics into policy optimization by leveraging empirical residuals. We show that this Bellman operator is a contraction mapping and identify conditions under which its fixed point is asymptotically optimal and possesses finite-sample guarantees. We further develop a residuals-based offline deep Q-learning (DQN) algorithm. Using a stochastic CartPole environment, we demonstrate the effectiveness of our residuals-based offline DQN algorithm.

---

## 论文详细总结（自动生成）

这是一份关于论文《Residuals-based Offline Reinforcement Learning》的结构化深入分析报告：

### 1. 核心问题与研究背景
*   **核心问题**：离线强化学习（Offline RL）在实际应用中面临三大挑战：
    1.  **数据覆盖限制**：通常要求离线数据集必须覆盖目标策略可能访问的所有状态-动作空间，这在连续空间中极难实现。
    2.  **反事实查询困难**：无法在离线状态下准确回答“如果采取了不同动作会发生什么”。
    3.  **分布偏移（Distribution Shift）**：训练策略与行为策略不一致，导致价值函数评估不可靠。
*   **研究动机**：借鉴上下文随机优化中的“经验残差样本平均近似（ER-SAA）”思想，探索如何将环境动力学学习中的**估计误差**显式地引入到策略优化过程中，从而缓解上述问题。

### 2. 方法论
*   **核心思想**：不直接使用点预测（Point Prediction）来模拟环境，而是通过“回归模型 + 经验残差采样”的方式构建一个模拟环境，使智能体在模拟出的“准真实”动态中进行学习。
*   **关键技术细节**：
    1.  **回归阶段**：利用离线数据集 $D_N$ 训练一个转移模型 $\hat{f}_N(s, a)$，预测下一状态。
    2.  **残差构建**：计算训练集中的经验残差 $\hat{\epsilon}^{(i)}_N = s'^{(i)} - \hat{f}_N(s^{(i)}, a^{(i)})$，形成残差池。
    3.  **基于残差的贝尔曼算子 ($\hat{T}_N$)**：定义新的算子，在计算期望价值时，通过从残差池中采样并叠加到预测值上：$s'_{next} = \Pi_S(\hat{f}_N(s, a) + \hat{\epsilon})$。
    4.  **理论证明**：论文证明了该算子是 $\gamma$-收缩映射，保证了不动点的唯一性；并给出了其在无限样本下的渐近最优性和有限样本下的误差界。
*   **算法流程**：提出了 **Residuals-based Offline DQN**。在训练时，智能体在由 $\hat{f}_N$ 和残差池构成的模拟器中进行“在线”训练，从而实现对未见状态的探索和对分布偏移的修正。

### 3. 实验设计
*   **实验场景**：随机倒立摆（Stochastic CartPole）环境。在标准环境基础上，对小车位置观测加入了高斯噪声 $N(0, 0.25)$ 以增强随机性。
*   **Benchmark（对比基准）**：
    *   **无残差模型（w/o residuals）**：仅使用回归模型 $\hat{f}_N$ 的点预测进行离线训练。
    *   **不同样本量对比**：测试了从 500 到 1000 条不同轨迹数量下的性能表现。
*   **对比方法**：主要对比了“有残差”与“无残差”两种框架在真实环境（测试环境）中的奖励表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件型号（如 GPU 类型）或总训练时长。
*   **网络参数**：
    *   回归模型：单隐藏层神经网络，64 个神经元，ReLU 激活。
    *   Q 网络：两个隐藏层，每层 64 个神经元，ReLU 激活。
    *   优化器：Adam，学习率 $10^{-4}$，折扣因子 $\gamma=0.99$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 6 种不同的样本量（500, 600, ..., 1000）进行了测试。
    *   每组实验均进行了 **5 次独立运行**并报告了平均结果。
*   **充分性评价**：实验设计较为基础。虽然通过消融实验验证了残差项的关键作用，且样本量敏感性分析符合预期，但实验仅限于 CartPole 这一单一简单场景，缺乏在更复杂（如高维动作空间或多任务）基准数据集（如 D4RL）上的验证。

### 6. 主要结论与发现
1.  **残差的重要性**：不带残差的模型虽然在训练阶段表现良好，但在真实环境中表现极差，说明忽略估计误差会导致严重的模型偏差。
2.  **性能优势**：基于残差的方法在测试奖励上显著优于纯回归方法，能够更准确地捕捉底层随机转移特性。
3.  **数据缩放效应**：随着离线数据量的增加，基于残差的策略性能稳步提升，验证了算法的收敛性和一致性。

### 7. 优点
*   **理论支撑完备**：不仅提出了算法，还提供了详尽的数学证明（收缩映射、Lipschitz 连续性、有限样本保证），这在强化学习论文中较为扎实。
*   **框架通用性**：该方法不依赖于特定的策略梯度或价值迭代算法，可以作为一种通用的“环境模拟层”集成到多种离线 RL 算法中。
*   **缓解覆盖问题**：通过残差采样，算法能够生成数据集中未直接出现的邻近状态，天然地增强了算法的鲁棒性。

### 8. 不足与局限
*   **实验覆盖面窄**：仅在 CartPole 环境验证，未在主流的离线 RL Benchmark（如 MuJoCo 任务）上进行大规模测试。
*   **噪声独立性假设**：理论分析中对残差的性质有一定假设（如独立性），在具有异方差噪声（噪声随状态动作变化）的复杂系统中，简单的残差叠加可能失效。
*   **计算复杂度**：在每个贝尔曼更新步骤中引入残差采样和投影操作，在大规模状态空间下可能会显著增加计算开销。

（完）
