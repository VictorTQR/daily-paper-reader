---
title: Truncated Rectified Flow Policy for Reinforcement Learning with One-Step Sampling
title_zh: 用于单步采样强化学习的截断修正流策略
authors: "Xubin Zhou, Yipeng Yang, Zhan Li"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09159v1"
tags: ["query:rl-agt"]
score: 9.0
evidence: 提出了用于一步采样强化学习的截断修正流策略。
tldr: 针对最大熵强化学习中高斯策略难以处理多峰分布且生成式策略采样慢、优化难的问题，本文提出截断修正流策略（TRFP）。该方法采用确定性与随机性混合架构，结合梯度截断和流直化技术，实现了可计算的熵正则化优化和高效的一步采样。实验证明 TRFP 在多目标及 MuJoCo 任务中表现优异，兼顾了表达能力与推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09159v1/fig-001.webp\", \"caption\": \"Table 1: Final return comparison on 10 MuJoCo-v5 benchmarks. Results are reported as mean ± standard deviation. Best results are shown in bold, and second-best distinct results are underlined. NFE denotes the number of function evaluations per action. For iterative generative policies, expressions of the form a× b indicate a rollout steps with b sampled action candidates.\", \"page\": 10, \"index\": 1, \"width\": 818, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2604.09159v1/fig-002.webp\", \"caption\": \"Figure 3: Ablation results of TRFP. From left to right: the effect of removing flow straightening regularization, the effect of removing Q-guided action selection, and the effect of removing the stochastic tail. The first two panels are evaluated on HalfCheetah-v5, and the third panel is evaluated on Humanoid-v5. Solid lines denote the mean episode return over five random seeds, and shaded regions represent one standard deviation.\", \"page\": 10, \"index\": 2, \"width\": 964, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2604.09159v1/fig-003.webp\", \"caption\": \"Figure 2: Learning curves on 10 MuJoCo benchmarks. Each subplot compares TRFP under standard sampling (K = 4) and one-step sampling (K = 1) with four baselines. The solid lines denote the mean episode return over five random seeds, and the shaded regions represent one standard deviation.\", \"page\": 9, \"index\": 3, \"width\": 964, \"height\": 1151}, {\"url\": \"assets/figures/arxiv/2604.09159v1/fig-004.webp\", \"caption\": \"Figure 1: Trajectory visualization in the toy multigoal environment. The agent starts near the center and moves toward four equally rewarded goals. Each panel overlays 20 evaluation episodes collected after training.\", \"page\": 8, \"index\": 4, \"width\": 976, \"height\": 341}]"
motivation: 传统高斯策略难以捕捉复杂的多峰动作分布，而现有的生成式策略在强化学习中面临似然计算难、训练不稳定及推理延迟高的问题。
method: 提出一种基于混合架构的截断修正流策略，通过梯度截断和流直化技术实现稳定的熵正则化优化及高效的一步采样。
result: 在多目标环境和10个MuJoCo基准测试中，TRFP 成功捕捉了多峰行为，且在标准及一步采样下均优于或持平于强基线模型。
conclusion: TRFP 为强化学习提供了一种兼具高表达能力、训练稳定性和极速推理能力的生成式策略框架。
---

## 摘要
最大熵强化学习（MaxEnt RL）已成为序列决策的标准框架，但其标准的高斯策略参数化本质上是单峰的，限制了其建模复杂多峰动作分布的能力。这一局限性激发了人们对基于扩散（diffusion）和流匹配（flow matching）的生成式策略作为更具表达力替代方案的日益关注。然而，将此类策略整合到 MaxEnt RL 中面临两个主要挑战：连续时间生成式策略的似然和熵通常是难解的，且多步采样会引入长时界反向传播的不稳定性和显著的推理延迟。为了应对这些挑战，我们提出了截断修正流策略（Truncated Rectified Flow Policy, TRFP），这是一个基于确定性-随机混合架构的框架。该设计使熵正则化优化变得可行，同时通过梯度截断和流直化支持稳定的训练和有效的单步采样。在简单多目标环境和 10 个 MuJoCo 基准测试上的实验结果表明，TRFP 能够有效捕捉多峰行为，在标准采样下于大多数基准测试中优于强基线模型，并且在单步采样下仍保持极强的竞争力。

## Abstract
Maximum entropy reinforcement learning (MaxEnt RL) has become a standard framework for sequential decision making, yet its standard Gaussian policy parameterization is inherently unimodal, limiting its ability to model complex multimodal action distributions. This limitation has motivated increasing interest in generative policies based on diffusion and flow matching as more expressive alternatives. However, incorporating such policies into MaxEnt RL is challenging for two main reasons: the likelihood and entropy of continuous-time generative policies are generally intractable, and multi-step sampling introduces both long-horizon backpropagation instability and substantial inference latency. To address these challenges, we propose Truncated Rectified Flow Policy (TRFP), a framework built on a hybrid deterministic-stochastic architecture. This design makes entropy-regularized optimization tractable while supporting stable training and effective one-step sampling through gradient truncation and flow straightening. Empirical results on a toy multigoal environment and 10 MuJoCo benchmarks show that TRFP captures multimodal behavior effectively, outperforms strong baselines on most benchmarks under standard sampling, and remains highly competitive under one-step sampling.

---

## 论文详细总结（自动生成）

这是一份关于论文《Truncated Rectified Flow Policy for Reinforcement Learning with One-Step Sampling》（TRFP）的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
最大熵强化学习（MaxEnt RL）通过引入熵正则化来提升智能体的探索能力和鲁棒性。然而，传统的 MaxEnt RL（如 SAC）通常采用**高斯策略**，其单峰特性限制了在复杂、多峰动作分布场景下的表现。
虽然扩散模型（Diffusion）和流匹配（Flow Matching）等生成式模型具有极强的表达能力，但将它们引入在线 MaxEnt RL 面临三大挑战：
*   **似然计算难解**：连续时间生成模型的精确对数似然（用于计算熵）需要复杂的积分或雅可比行列式计算，在线训练开销巨大。
*   **训练不稳定**：多步采样过程类似于深度循环神经网络，直接进行全路径反向传播（BPTT）容易导致梯度消失或爆炸。
*   **推理延迟高**：生成式模型通常需要多步迭代，难以满足实时控制的需求。

### 2. 方法论：核心思想与关键技术
TRFP 提出了一种**混合确定性-随机架构**，通过解耦训练和流直化技术解决上述问题：
*   **混合策略架构**：将动作生成过程分为两阶段。前段（Prefix）采用确定性常微分方程（ODE）进行快速输运；末段（Tail）采用随机微分方程（SDE）进行细化。
*   **代理熵与似然**：仅针对 SDE 阶段构建可计算的代理对数似然（Surrogate Log-likelihood），避开了对 ODE 阶段复杂体积变化的计算。理论证明，当流场散度较小时，该近似误差受控。
*   **截断梯度估计**：在 ODE 和 SDE 的交界处截断梯度。RL 的价值梯度仅通过短步长的 SDE 阶段回传，从而避免了长链 BPTT 的不稳定性。
*   **流直化正则化（Flow Straightening）**：引入辅助任务，利用自蒸馏技术强制 ODE 阶段的轨迹趋于直线。这不仅减小了代理似然的误差，还使得单步采样（One-step sampling）成为可能。
*   **Q 引导的动作选择**：在推理时并行生成多个候选动作，利用 Critic 网络选择 Q 值最高的动作，进一步提升性能。

### 3. 实验设计
*   **实验场景**：
    *   **Toy Multigoal**：一个具有四个对称目标的简单环境，用于验证模型捕捉多峰分布的能力。
    *   **MuJoCo 基准测试**：包含 10 个标准的连续控制任务（如 Humanoid-v5, HalfCheetah-v5, Walker2d-v5 等）。
*   **对比基线（Baselines）**：
    *   经典算法：**SAC**（高斯策略）、**TD3**（确定性策略）。
    *   生成式策略前沿算法：**SDAC**、**MaxEntDP**（基于扩散模型的 MaxEnt RL）。

### 4. 资源与算力
论文明确指出了实验所使用的硬件环境：
*   **GPU**：单张 NVIDIA RTX GeForce 4090。
*   **CPU**：Intel Core i9-14900KF。
*   **训练细节**：使用了 Mish 激活函数，Actor 和 Critic 均采用三层 MLP 架构。具体的训练时长未详细说明，但给出了 1e6 步的训练曲线。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同的 MuJoCo 任务上进行了测试，每个任务运行了 **5 个随机种子**，并给出了均值和标准差。
*   **消融实验**：针对“流直化正则化”、“Q 引导选择”以及“随机尾部（Stochastic Tail）”分别在 HalfCheetah 和 Humanoid 环境下做了消融对比。
*   **充分性评价**：实验设计较为全面，涵盖了从简单多峰验证到复杂高维控制任务的跨度。通过与多种生成式策略基线的对比，证明了其在采样效率（NFE）和最终性能上的双重优势，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **多峰建模能力**：TRFP 在多目标环境中能均匀覆盖所有目标，而 SAC 和 TD3 往往陷入局部最优（单峰）。
*   **性能领先**：在大多数 MuJoCo 任务中，TRFP 的最终回报优于或持平于现有的扩散策略（如 SDAC）和经典算法。
*   **极速推理**：得益于流直化技术，TRFP 在**单步采样（K=1）**下的性能损失极小，且依然优于许多需要多步采样的基线模型。
*   **训练稳定性**：截断梯度机制有效解决了生成式策略在线训练时的梯度病态问题。

### 7. 优点与亮点
*   **架构优雅**：通过 ODE-SDE 混合设计巧妙地平衡了“似然可计算性”与“生成表达力”。
*   **推理效率极高**：无需额外的蒸馏步骤即可实现高质量的一步采样，这在生成式策略研究中是一个显著突破。
*   **解耦优化**：将 RL 目标与流匹配目标结合，既保证了价值最大化，又保证了生成轨迹的几何特性。

### 8. 不足与局限
*   **近似误差**：代理对数似然本质上仍是近似，虽然有理论边界，但在极高维或极复杂流场下可能存在偏差。
*   **任务覆盖**：目前主要集中在 MuJoCo 模拟环境，尚未在更大规模的机器人操纵（Robot Manipulation）或长时界规划（Long-horizon planning）任务中验证。
*   **超参数敏感性**：引入了混合架构和辅助损失函数，可能增加了超参数调优（如截断位置 L、正则化权重 $\lambda_{fm}$）的复杂度。

（完）
